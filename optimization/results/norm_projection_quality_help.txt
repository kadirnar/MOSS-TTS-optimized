usage: quality_generate.py [-h] --mode {none,fp8_all} --tag TAG
                           [--fused-residual] [--fused-gateup]
                           [--attention-block ATTENTION_BLOCK] [--fp8-prefill]
                           [--prefix-cache PREFIX_CACHE]
                           [--calibration CALIBRATION]
                           [--calibrated-backend {marlin,dp4a}]
                           [--dp4a-reciprocal] [--fused-dp4a]
                           [--grouped-activation]
                           [--prefill-buckets PREFILL_BUCKETS [PREFILL_BUCKETS ...]]
                           [--dp4a-gateup] [--norm-layout-limit {0,8}]
                           [--packing-plan PACKING_PLAN] [--decode-buckets]
                           [--attention-quant] [--native-attention]
                           [--gateup-quant] [--scaled-dp4a]
                           [--norm-projection] [--audio-head-buckets]

options:
  -h, --help            show this help message and exit
  --mode {none,fp8_all}
  --tag TAG
  --fused-residual
  --fused-gateup
  --attention-block ATTENTION_BLOCK
  --fp8-prefill
  --prefix-cache PREFIX_CACHE
  --calibration CALIBRATION
  --calibrated-backend {marlin,dp4a}
  --dp4a-reciprocal
  --fused-dp4a
  --grouped-activation
  --prefill-buckets PREFILL_BUCKETS [PREFILL_BUCKETS ...]
  --dp4a-gateup
  --norm-layout-limit {0,8}
  --packing-plan PACKING_PLAN
  --decode-buckets
  --attention-quant
  --native-attention
  --gateup-quant
  --scaled-dp4a
  --norm-projection
  --audio-head-buckets
