# Continuing optimization objective

The active goal is still 50 ms streamed TTFA for MOSS-TTS-v1.5, including voice cloning. Review and improve both the codec and LLM, test current serving libraries, inspect current documentation and recent research, and investigate hardware-level fusion and parallel execution. Preserve all 32 codebooks as requested. Every experiment below retains all 32. See `REPORT_COMPRESSION.md` for the latest trials and `REPORT_REVIEW.md` for the unchanged running services; earlier pending items below are historical.

The preceding optimization turn made progress: new full-model FP8/INT8/INT4 trials, CUDA kernel validation, FlashInfer integration and measurements, profiles and reproducible reports. It did not reach 50 ms or finish full serving-engine comparisons. The goal remains active.

## Current evidence and work

- Historical BF16/FP32 service: about 174 ms HTTP TTFA; fastest earlier experimental FP8 path: 135.5 ms with a cached voice reference. Revalidate against new changes rather than treating these as current success.
- Checked PyPI package metadata and saved `results/latest_versions.json`. Installed SGLang-Omni 0.1.6 in `/venv/moss-latest` and vLLM 0.29.0 with vLLM-Omni 0.28.0 in `/venv/moss-vllm`. Their numba pins require separate environments. The running service uses `/venv/main`.
- Marlin's real packed-weight CUDA kernels are being tested; the earlier naive INT4 failures do not establish a hardware or quantization limit.
- Current-runtime FP8 (Torch 2.13.0+cu130, Triton 3.7.1): 131.341 ms TTFA, 3.480 ms decode. Attention block 32 / four warps reduced these to 127.001 ms and 3.386 ms. All 32 codebooks, FP32 codec, cached reference, complete streamed utterances; diagnostic quality only. See `all32_fp8_all_latest_{baseline,tuned_b32w4}.json` and profiles.
- Attention tuning covered 40 configurations/contexts with cold-L2 timing and numerical checks. Short-context block 32 beats 128; the complete-model improvement above was measured separately.
- SGLang-Omni 0.1.6 / SGLang 0.5.19 complete streaming HTTP: median 423.385 ms, p95 464.949 ms, five warm cloned-voice requests, all 32 / FP32 codec. The first 10.6-second lazy-init request was excluded. Reference data-URI upload is included; this is a different boundary from in-process cached-reference timing.
- vLLM 0.29.0 is incompatible with vLLM-Omni 0.28.0's CLI imports. Created `/venv/moss-vllm-omni` with the matched 0.28.0 pair for full-engine tests. The 0.29 environment remains useful for native-kernel tests.
- Vanilla vLLM-Omni 0.28's MOSS v1 streaming codec fails startup because the v1 vendored codec has no decoder state pool (only v2 implements it). Added an explicitly gated single-stream adapter using our original-checkpoint FP32 causal codec. This is a hybrid benchmark, not an unmodified-library result; `install_vllm_codec_adapter.py` installs the hook only in the isolated environment, enabled by `MOSS_V1_OPTIMIZED_CODEC=1`.
- Native Marlin W4/G128: 133.920 ms TTFA but one run truncates, active top-1 agreement 0.445. W4/G32 on current vLLM: 136.144 ms, active top-1 agreement 0.539. Neither passes a quality acceptance gate. Native CUTLASS FP8 was slower than custom weight-only FP8 on all four actual projection shapes, so it was not promoted.
- New Transformers StaticCache appends using its internal counter instead of the explicit cache position. Fixed our owned prefill cache updates to index the requested positions directly. Latest-runtime full-model checks passed; original-runtime BF16 regression remains to run.
- Mirage source commit `1f3338f9ae084559726fb1fc898ae077ff7d171a` built successfully with CUDA 12.8. Added an experimental online_notoken adapter for the same 36-layer backbone. Validation and timing remain pending.
- GemLite source installed. Initial tests correctly rejected large split-K numerical error. FP32 local accumulation alone is insufficient because BF16 atomic output reduction rounds partial sums; the next trial disables BF16 atomics too. No model integration until projection checks pass.
- Broader voice-cloning quality checks, persistent-kernel experiments, original-runtime regression, and the 50 ms target remain open. Every experiment so far retains all 32 codebooks.

## Subsequent measurements and fixes

- The new prefill cache update passed the original-runtime BF16 regression: 175.677 ms median, logit relative RMS 0.007718 and active top-1 0.921844, identical numerical metrics to the historical implementation.
- Fused residual addition + RMSNorm retains both BF16 rounding boundaries. Exact component comparisons passed on a non-default CUDA stream under graph replay. Full FP8 with short attention tiles improved from 127.001 to **123.408 ms TTFA** (p95 124.020; decode 3.293 ms), with unchanged teacher-forced metrics. Original-runtime BF16 with these two changes measures **166.822 ms**.
- vLLM-Omni's default delay adapter explicitly uses a billion-frame threshold and waits until utterance completion. Its codec-adapted initial measurement was 1448.167 ms and did not provide incremental generation. Added a gated incremental delay adapter and 18 exact synthetic frame-order/padding/drain cases. The repaired full HTTP pipeline now measures **367.544 ms median / 370.037 ms p95**, streaming before utterance completion, full 32 / FP32 codec. Artifacts `vllm_omni028_incremental_fp32.*`.
- An earlier vLLM measurement accidentally treated SSE text as PCM. Those artifacts are explicitly marked `.invalid.*`; the benchmark now rejects text/JSON content types and vLLM uses `stream_format=audio`. Do not cite the invalid artifacts as latency or audio quality evidence.
- Mirage's online_notoken mode launches on private streams without joining the calling CUDA stream. The first attempted comparison read intermediate activations too early and is marked `.invalid_async.*`. With its explicit wait API, the unchanged BF16 backbone passes: max logit relative RMS **0.007000**, active top-1 **0.892755**, but backbone-only latency **5.625 ms**, slower than the custom full decode. No streaming promotion.
- GemLite's correct FP32 local and atomic accumulation passed all 12 projection checks, relative RMS at most 0.000222 against dequantized BF16. Its W4/G32 timings are 39.055 / 22.389 / 13.913 / 10.827 microseconds across gate-up/down/QKV/output shapes; slower than both Marlin and current FP8. No full-model promotion.
- A fused FP8 gate/up + BF16-rounded SiLU kernel is numerically exact with four warps and lowers the isolated projection timing to 28.063 microseconds. Full-model and BF16 variant trials are pending.
- Codec high/TF32 matmul policy reduced 140-frame median decode from 4.899 to 4.548 ms, with 60.390 dB waveform SNR versus IEEE FP32. Medium gave the same result. This small precision tradeoff is not enabled by default.
- A 16-utterance bilingual/four-reference generation and ASR/speaker-vector evaluation harness is ready. Microsoft WavLM speaker model is pinned to `feb593a6c23c1cc3d9510425c29b0a14d2b07b1e`. This will be a broader diagnostic, not a human-listening quality guarantee.

## Latest completed checks and deployment

- Gate/up/SiLU fusion reduced current-runtime FP8 TTFA to **118.142 ms**, decode 3.121 ms. Both fusions preserve the preceding configuration's teacher-forced metrics. The latest profile places 62.7% of GPU time in weight GEMV and fused gate/up/SiLU.
- BF16 with original attention block 128 and both fusions measured **170.958 ms** in process. All 16 bilingual/four-reference WAVs match the previous custom BF16 suite byte-for-byte. This configuration now runs in `moss_tts_optimized` on local port 18080.
- The separately running FP8 experimental service on local port 18083 measured **122.170 ms median / 125.273 ms p95 HTTP TTFA**, 20 complete requests. The updated BF16 service measured **175.449 / 178.229 ms**. Both passed reference registration, validation errors and cancellation/recovery; the current BF16 HTTP run is not faster than the historical HTTP run.
- Normalized diagnostic Chinese CER: prior BF16 4.46%, BF16 block 32 plus fusions 6.95%, FP8 block 32 plus fusions 7.59%. Speaker mean cosine: 0.9104, 0.9163, 0.9090. All sampled utterances finish; all identify the intended reference among the four candidates. FP8 and the changed attention reduction remain experimental. The selected default retains block 128 and identical sampled WAVs.
- ExLlama CUDA INT4 and PyTorch tinygemm passed 16 projection checks but showed no clear speed advantage justifying a full-model quality tradeoff. Split-K Triton FP8 passed 162 projection/configuration checks, but all three shapes were slower than the original kernels; not integrated.
- Current FP8 fresh-reference TTFA is **138.793 ms**, including **20.522 ms** reference encoding from a decoded 3.112-second waveform. Cached-reference prompt cases measured 118.05–118.62 ms. No synthesized output is cached.
- `REPORT_REVIEW.md` consolidates the measured scope, quality limitations, full-engine comparisons and reproducible artifacts. The 50 ms goal remains unmet and active.
- Newer-runtime BF16 with both fusions measured 166.354 ms, but normalized Chinese CER was 8.51% and mean speaker cosine 0.9072 on the same 16 samples. All finish and match the intended reference. Keep this evaluation separate; do not promote the runtime merely for its small speed gain.
- Fusing Q/K normalization, RoPE, cache writes and attention passed 18 non-default-stream output/KV checks. Each reader computes the current K/V locally, avoiding an inter-block write/read race. Block 32 at position 144 measured 15.20 us versus 15.15 us with cold L2; block 128 regressed. No model integration.

## Prefix and DP4A continuation (2026-09-20)

- Classified the preceding turn as progress: deployed exact BF16 fusions, completed full HTTP and quality checks, and retained slower trials with evidence. Current GPU and both local services were revalidated before further work. All 32 codebooks remain intact.
- Added opt-in conditioning-prefix KV reuse, keyed by all input channels and bounded before the verified text marker. Cached 96/120-token prefixes reduced FP8 TTFA to 115.45/115.42 ms. Changed text/language/voice, long prompts, no-reference bypass, cleared KV and a non-default stream were checked.
- Forty W8A8 prefill operator checks across 20 shapes/token counts selected vLLM CUTLASS for prefill; custom Triton remains faster for single-token decode. Full W8A8 prefill measured 116.90 ms; combining it with the 120-token prefix measured **114.11 ms median / 114.40 ms p95**, five complete requests, all 32, FP32 codec.
- The combined variant's 16-utterance/four-reference diagnostic gave Chinese CER 8.51%, English WER 0.78%, mean speaker cosine 0.9106, all intended reference matches and no truncation. It remains experimental and is not deployed, because Chinese CER increased versus the previous FP8 trial's 7.59%.
- Built and checked native FP6-LLM (SM90, source `12e83379f16a4ee1494be00db6956aab56baf620`). All 24 checks pass, but kernels are slower; no model integration.
- Nsight Compute hardware counters are denied by the host. Compiler inspection remains available: the fused FP8 kernel has 38 registers/thread and no spills. PTX exposed scalar activation loads in the grouped INT4 trial. Explicit packed BF16 reads improved its G32 gate-up/down to 37.71/20.71 microseconds; 40 checks passed for each load variant.
- DP4A with explicit rounded activation division passed 32 projection/warp checks. The full G32 round-to-nearest model measured 127.51 ms but had one token-limit truncation and low teacher-forced agreement (0.425). Not selected. The first quantizer-mismatch trial is marked invalid; no failed trial is cited as a speedup.
- `REPORT_PREFILL.md` records definitions, comparisons, quality and reproduction. Both running services remain on the previous validated configurations. The 50 ms objective remains active.
- Explicit reciprocal-rounding DP4A passed 32 independent-reference checks and improved G32 gate-up/down/QKV/output to 22.95/15.42/9.85/7.99 microseconds; full-model reciprocal testing and calibrated weight quantization remain open. The default BF16 full-model regression after all edits completed five requests with the same recorded numerical metrics as before.

## Calibrated integer-kernel continuation (2026-09-20)

- The previous pass was progress, not a blocker. Both existing services remain running and all 32 codebooks are preserved. The 50 ms goal remains active.
- Collected 12 complete BF16 calibration utterances with evaluation-disjoint texts, using the four supplied voice assets. Eight utterances / 866 decode rows train GPTQ; four / 410 rows are held out for calibration diagnostics. All 144 projection exports are separate from the fixed source checkpoint.
- Implemented static-group, activation-ordered GPTQ error feedback with a fused Triton column update. Six algorithm comparisons matched the Torch integer codes exactly. Twenty-four real packed-backend checks passed. Global damping 0.1, selected by a 12-projection pilot, improved held-out output MSE in 139/144 full-export projections; median error ratio versus RTN was 0.442.
- Calibrated Marlin measured 130.28 ms TTFA. Calibrated per-vector DP4A with explicitly defined reciprocal rounding measured 113.60 ms. Grouped activation quantization recovered teacher-forced top-1 agreement from 0.525 to 0.647 and measured 111.56 ms. No measured request truncated.
- Fused normalization/quantization reduced grouped DP4A to 109.12 ms; paired DP4A gate/up/SiLU to 106.21 ms; adding a 160-token prefill bucket for the 145-token fixture reached 104.65 ms / p95 105.22. All retain BF16 prefill and the FP32 codec. The separate SiLU/quantizer fusion was slower and is disabled.
- Quality diagnostics: calibrated Marlin Chinese CER 5.51%; global-activation DP4A 15.46%; grouped-activation DP4A 8.05%; original fused/bucket variant 3.80%; layout-constrained fused/bucket variant 5.37%. All have normalized English WER 0%, intended speaker-reference top-1 in 16/16 cases and no truncation. These remain small, repeatedly explored diagnostics, not broad quality acceptance.
- Synthetic normalization-fusion checks passed exactly, but a subsequent two-utterance real-activation audit exposed 223 BF16 differences, 76 integer-code differences and four scale differences. Preserve this finding: the original fused variant is numerically changed. Compiler IR exposed a 16-element per-thread reduction layout versus eight in the original. Constraining contiguity to eight restored exact real-activation agreement and cut shared memory from 4096 to 512 bytes. The separately measured corrected path reached 106.43 ms / p95 106.69.
- The post-change BF16 regression completed five requests at 171.23 ms and exactly matched the previous recorded teacher-forced metrics. An explicit calibrated preset was added to the same streaming HTTP API; both existing services remain on their original configurations.
- The layout-constrained calibrated preset passed temporary local HTTP validation: 101.53 ms median / 103.94 ms p95 over 20 warm cloned-voice requests; one contiguous fresh voice-registration-plus-synthesis observation was 140.44 ms. This shorter HTTP text differs from the 145-token in-process fixture. Voice/error/cancellation/recovery checks passed. A benchmark iterator-drain mistake was fixed and its failed log retained; only the successful rerun supplies reported HTTP timings. The temporary endpoint was stopped after testing.
- `REPORT_CALIBRATED.md` records methods, latency boundaries, quality, failures and reproduction. Projection kernels remain the principal GPU bottleneck. No evidence establishes an absolute 50 ms lower bound.

## Packed-weight continuation (2026-09-20)

- Classified the previous turn as progress and revalidated the H200, idle GPU and existing services. The target remains unmet; all 32 codebooks are preserved.
- Tested PTX byte-permutation expansion and an interleaved eight-value INT4 layout, plus BF16 storage of exactly representable scales. All 192 operator configurations passed tolerance checks. Exhaustive checks cover 65,536 signed four-nibble words and 65,536 packed-layout rows; 72 real/edge projection checks passed.
- Fastest interleaving reduced complete-model TTFA to 95.76 ms, but changed the 16 evaluation WAVs and regressed Chinese CER from 5.37% to 7.59%. This numerical variant remains a recorded experiment.
- Added opt-in attention capacities 128/256/512 with the full 1024-token fallback, selected from the actual position. All 16 attention/KV tests and 12 full-backbone boundary/return/fallback checks matched exactly, including a private stream. Combining buckets with the fast packing measured 94.69 ms.
- Filtered packing plans by exact real/edge outputs. The selected plan uses interleaving with row/warp counts QKV 4/4, output 2/2, gate-up 4/4, down 2/2. All 72 checks match. Compiler inspection found 40/48/40/125 registers per thread and no spills; shared memory and PTX/IR are saved.
- The selected matching plan plus decode buckets measured 98.69 ms / p95 100.32 in-process TTFA, five complete requests. It recovered the previous calibrated path's recorded teacher-forced metrics. All 16 generated WAVs, four reference WAVs and prompt/seed/voice/frame metadata match that earlier path exactly. Its existing 5.37% Chinese CER / 0% English WER / 0.9143 speaker cosine diagnostics apply to the identical files.
- The default BF16 regression completed five requests at 171.26 ms and matched the previous validation and saved WAV exactly. Optional packing and decode-bucket flags are wired into the streaming server; temporary local HTTP validation is recorded in `REPORT_PACKED.md`.
- The initial matching-plan HTTP run was 101.34 ms / p95 104.83. Stage instrumentation and fresh sequential servers then measured calibrated 107.58 ms versus packed 100.25 ms / p95 101.17 over 20 complete requests per configuration. Decode fell from 2.759 to 2.526 ms; prefill and codec costs were similar. Both passed cancellation/recovery. Fresh registration-to-PCM single observations were 152.93 and 145.34 ms. Temporary servers were stopped.
- Correction to the preceding calibrated continuation: direct inspection confirms HTTP and the in-process fixture use identical text and 145 prompt tokens. The earlier shorter-text explanation was incorrect. Historical timing artifacts are retained; a controlled comparison is required to distinguish runtime variability from HTTP overhead.
- Projection arithmetic still dominates. Remaining avenues include the high-register MLP-down kernel, reducing work on masked output heads, and measured draft/verification acceptance. Recent speculation papers and a different block-wise TTS architecture were reviewed; their reported latencies are not measurements of this checkpoint. No proof supports reducing codebooks under the conditional goal clause.

## Direct-load kernel continuation (2026-09-20)

- HTTP stage instrumentation showed similar preparation, prefill and codec costs for packed and preceding calibrated paths. The largest remaining cost was the LLM projections. No codebook reduction was made.
- Inline PTX now optionally performs activation loads and both DP4A operations in the weight register layout. Forty-eight row/warp variants passed tolerance checks on actual calibrated projections. The pre-quality selection threshold required 18/18 exact checks and more than 2% operator gain; QKV and MLP down qualified. Their cold operator medians fell from 8.25/14.21 to 7.97/12.02 microseconds. Neither selected kernel spills; compiler PTX shows fewer barriers and shared-memory transfers.
- The first five-request full-model run had a 144.55 ms outlier, including one 52.50 ms decode step; median 94.64 / p95 134.94 ms. It is retained with cause unknown. A ten-request repeat measured 94.04 / p95 96.07 ms, all complete. Recorded teacher-forced diagnostics and saved WAV match the prior calibrated/packed path exactly.
- All 16 bilingual generated WAVs and four reference WAVs are byte-identical to the prior selected path. Metadata matches; existing CER 5.37%, WER 0%, speaker cosine 0.9143 apply to those identical files. This remains quantized-model diagnostic evidence, not BF16 equivalence or broad quality acceptance.
- Compute Sanitizer memcheck passed eight odd-row/padded-K paired/unpaired checks on a private stream with zero errors. Full-model CUDA-graph execution was measured separately, without sanitizer overhead.
- The direct-load HTTP trial measured 91.11 / p95 93.29 ms across 20 complete cached-voice requests; fresh registration-to-PCM was 130.33 ms in one observation. Cancellation recovered through 429 then 200. The repeated preceding-plan control measured 93.21 / p95 101.70 ms, versus its earlier 100.25 ms median. The closer comparison therefore supports a 2.10 ms HTTP gain; the earlier 9.14 ms difference includes unexplained timing variation. All control/tail measurements are retained. Temporary servers were stopped, existing supervisor services unchanged.
- `REPORT_PACKED.md` records the complete iteration, correction, source hashes, numerical checks and reproducible commands. Projection kernels still consume about half of GPU time. The next iteration can inspect scale-load layout conversions or quantify masked-head/draft opportunities, preserving all 32. The 50 ms objective remains active and unmet.

## Attention epilogue and latest compiler continuation (2026-09-20)

- Classified the preceding turn as progress; checked the current worktree, idle H200 and both existing services. All 32 codebooks remain enabled, and 50 ms remains unmet.
- Tested 192 scale-load/layout combinations and 144 explicit Gluon layouts on actual calibrated projections, with additional real/edge exact comparisons. No configuration passed both the exact-output requirement and 2% gain threshold. Gluon removed most shared-memory transfers but its exact configurations were slower. Both sets remain reproducible experiments, with failed initial compilation logs preserved separately.
- Fused grouped activation quantization into attention split reduction, removing 36 separate launches per decode step. Of 96 operator checks, only the four-warp configuration passed every BF16/INT8/scale exact comparison; it is the sole integrated choice. Sixteen full-backbone graph comparisons across boundaries, the 1024 fallback and returns to smaller positions matched all text/audio logits exactly on a private stream.
- Five complete requests measured 92.35 ms median / 92.54 ms p95 in-process TTFA. The 36-step diagnostics and saved WAV matched the preceding direct-load path. All 16 bilingual generated WAVs and four references matched byte for byte; prior CER 5.37%, WER 0% and speaker cosine 0.9143 therefore apply to those identical files.
- Tested the current Torch 2.14 / Triton 3.8 releases with matching CUDA dependencies in isolated `/venv/moss-kernels-latest`. It reached 89.71 ms / p95 90.02, but all 16 WAVs changed and normalized Chinese CER rose to 8.51%; English WER 0%, speaker cosine 0.9153, no truncation. Not selected as the quality-preserving option. A CPU-ASR protocol mismatch was interrupted; the matched CUDA-FP16 evaluator supplies reported scores.
- The isolated compiler-only `/venv/moss-triton38` experiment retains Torch 2.13 and cuDNN 9.20 while overriding the declared Triton pin. It reached 89.34 ms / p95 89.51 and reproduced the Torch 2.14 run's recorded diagnostics and single saved WAV. This isolates a compiler-related numerical change on the tested fixture; it is not a full vLLM/Inductor-compatible environment or separately quality-qualified variant.
- The new profile attributes the latest compiler's largest gain to attention (6.325 to 4.050 ms over 1,188 calls), while projections remain similar. A useful next investigation is cross-compiler operator comparison to identify the changed reduction and recover the speed without the observed corpus regression.
- Default BF16 regression completed five requests at 171.34 ms with identical prior validation and saved WAV. The matched HTTP comparison measured 97.97 → 96.10 ms median, p95 100.02 → 98.45 ms, with all 20 full PCM stream hashes identical. No truncation; cancellation/recovery passed. Fresh-reference single observations were 136.74 ms fused and 135.18 ms control, not evidence of an improvement. Both temporary endpoints were stopped. `REPORT_EPILOGUE.md` records evidence, source snapshots and commands. Existing services remain unchanged; target remains active.

## Native attention and compiler audit continuation (2026-09-20)

- Continued from the preceding progress, retaining all 32 codebooks. Captured 83 real operators and replayed 203 tensor checks (8,797,568 elements) under the original compiler with no differences. Compiler-only Triton 3.8 changed 23 projection elements, 75,370 attention partials and 87 log-sum-exp values; the other sampled families matched. PTX inspection identifies a changed register reduction tree/FMA placement in attention. This is sampled evidence, not an exhaustive compiler correctness claim or sole attribution of the earlier quality regression.
- Tested 16 Gluon attention layouts with 528 frozen/synthetic cases. Four-warp variants preserve the old arithmetic, but are slower. No Gluon variant was selected.
- Implemented native SM90 CUDA split attention with explicit old arithmetic order and separate shared buffers. It reduces static PTX barriers from 12 to 3. Scalar and vector-load versions each pass 33 frozen/boundary comparisons exactly, including partials, log-sum-exp, BF16 output, INT8 quantization and FP32 scales. Vector loads improve warm capacity-256 operator time from 4.39 to 2.88 microseconds. Both native versions use 40 registers, 2,080 shared bytes and no spills; source-addressed build artifacts retain both versions.
- Scalar full-model timing is 91.02 / p95 91.25 ms. A separate vector run is 92.51 / p95 93.19 ms despite a lower attention profile, demonstrating timing variation. Both reproduce prior teacher-forced diagnostics and saved WAV. Each also reproduces all 16 bilingual generated WAVs and four references byte for byte; existing 5.37% CER, 0% WER and 0.9143 speaker-cosine diagnostics apply to identical artifacts.
- Native memcheck and racecheck each pass 16 private-stream graph cases with poisoned future KV slots, reporting zero errors/hazards. Separate full-backbone graph checks pass for both versions across 16 positions, context boundaries, the 1024 fallback and returns to earlier positions. Defaults retain the prior Triton path; `--native-attention` is opt-in and specialized to SM90/B32/W4.
- Sequential HTTP servers measure 95.84 → 95.54 ms median, p95 96.85 → 97.55 ms; the 0.30 ms median change is small and the tail worsens. Engine median improves 92.18 → 91.02 ms, initial decode 2.391 → 2.336 ms. All 20 complete PCM hashes match, no truncation, input-error and cancellation/recovery checks pass. Fresh-reference single observations: 135.09 ms native, 136.09 ms control. Both temporary servers stopped; original supervisor processes unchanged.
- A more controlled same-process comparison alternates old/native complete graph sets with identical seeds and reverses order each pair. Ten measured pairs yield 92.23 → 90.55 ms median TTFA (p95 92.62 → 90.72), median paired gain 1.71 ms, all ten pairs faster by 1.39–2.15 ms. All ten complete float32 PCM streams match. This comparison excludes HTTP and fresh reference encoding and does not establish vector-vs-scalar full-model superiority.
- The current profile leaves projections as the principal bottleneck: 51.7% of GPU time, followed by fused norm/quant at 7.4%; native attention is 5.2%. Next iterations can measure gate/up plus subsequent activation-quantization fusion or native projection scheduling, retaining numerical and cloning checks. `REPORT_NATIVE_ATTENTION.md` records evidence, reproduction, source hashes and archived kernels. The 50 ms goal remains active; no absolute impossibility proof supports reducing codebooks.

## Gate/up quantizer fusion continuation (2026-09-20)

- Added opt-in Triton/direct-PTX gate/up/SiLU plus grouped activation-quantization fusion, retaining all 32 codebooks, BF16 prefill and FP32 codec. Across 59 recorded configurations, the selected row-32/four-warp layout takes 17.77 versus 19.32 microseconds. Expanded validation rejected the initially fastest unconstrained variant: 14 INT8, one scale and 3,606 downstream elements changed. Constraining scale contiguity to four recovers exact arithmetic across all 504 layer/input cases. Failing artifacts are retained.
- Private-stream graph memcheck passes eight padded/actual-shape cases with zero errors. Sixteen full-backbone graph checks across capacity boundaries/fallback and returns match all logits. All 16 bilingual cloning WAVs and four references match the preceding calibrated path byte for byte, preserving its small-suite diagnostics (CER 5.37%, WER 0%, speaker cosine 0.9143); this is not equality with original unquantized BF16 output.
- Independent five-request median is 89.04 ms. Ten same-process matched pairs measure **90.45 → 88.88 ms median** (p95 90.73 → 89.11), with 1.51 ms median paired gain and every pair faster. All complete float32 PCM streams match. The profile removes 1,188 standalone activation-quantizer launches; projections remain about 52.7% of GPU time.
- Twenty-request sequential HTTP comparison measures **95.79 → 88.86 ms median**, p95 96.91 → 90.45. All 20 PCM hashes/frame counts match, no truncation, input errors/cancellation/recovery pass. Unchanged prefill and codec also run faster in the candidate process: the whole 6.93 ms difference cannot be attributed to fusion. Fresh-registration single observations are 139.01 ms control and 135.56 ms candidate. Both temporary servers stopped; original services unchanged.
- A 27-configuration wider-projection sweep finds a small QKV operator improvement, exact across 504 additional cases. Ten paired complete-generation tests give only 0.097 ms median gain with five regressions, so wider QKV remains experimental. The selected plan stays row-4/four-warp.
- A five-mode NUMA test changes only the calling thread after model setup, rotating request order. GPU-local node 2 gives 93.02 ms versus unrestricted 91.77 ms; no placement improves. All same-seed PCM matches. Original affinity is restored, and serving is unchanged. This does not test startup memory placement or explain the earlier cross-process HTTP variation.
- Original-runtime BF16 regression completes five requests at 172.86 ms median, with the preceding validation dictionary and WAV exactly preserved. `REPORT_GATEUP_QUANT.md` records reproduction, numerical limitations and evidence. The target remains active and all codebooks remain enabled.

## Scaled-integer unpacking continuation (2026-09-20)

- Replaced explicit INT4 sign extension with signed bytes scaled by sixteen and an exact integer shift before FP32 conversion. The 62-configuration search finds improvements across all four projection families. Every selected operator passes 2,016 all-layer real/zero/spike cases exactly, including fused BF16 outputs, INT8 activations and FP32 scales. Two modes each pass 102,401 exhaustive/random integer dots and 16 private-stream graph memory checks in total; Compute Sanitizer reports zero errors. All selected kernels have zero spills.
- Five complete requests measure **87.61 ms median / 87.72 ms p95**. The previous 36-step diagnostic dictionary and saved WAV match exactly. Sixteen full-backbone private-stream boundary/fallback checks match all logits. All 16 bilingual generated WAVs and four references, plus generation metadata, match the preceding calibrated path. Existing small-suite quality scores apply to identical artifacts.
- Ten same-process pairs measure **88.91 → 87.67 ms median**, p95 89.00 → 87.89, with **1.23 ms median paired gain**. All ten PCM streams match and every pair improves. A late execution-speed shift affects unchanged prefill/codec and one pair spans it; the 6.87 ms outlier is retained without attributing it to unpacking. The 82.07 ms fastest observation is not the selected median.
- Sequential HTTP tests measure **94.81 → 90.24 ms median**, p95 97.51 → 90.69, with all 20 full PCM hashes/frame counts identical, no truncation, and cancellation/recovery passing. Preparation/seeding also vary, so the whole 4.57 ms difference is not a kernel-gain claim. Fresh-registration single observations are 141.52 / 131.81 ms. Both temporary servers stopped; original supervisor processes remain unchanged.
- A six-configuration shared activation-load experiment passes initial numerical checks but is slower (best 17.58 versus 17.06 microseconds), so it is not integrated. Current profile still places projections at 51.9% and normalization/quantization at 7.7%. Projection PTX retains numerous layout barriers; explicit native CUDA scheduling is a concrete next investigation.
- Isolated request-seed testing reduces the warm standalone call from 0.1143 to 0.0023 ms by seeding only the worker's current CUDA device. Ten same-process complete requests show 0.199 ms median paired gain with all PCM identical (nine improvements, one regression). Added opt-in `--cuda-only-seed`; all 20 HTTP PCM hashes/frame counts match and cancellation/recovery passes. The separate HTTP run is slower at 91.43 ms, so no HTTP gain is claimed; fresh-reference single observation is 131.20 ms. Global seeding remains the default, and the temporary server is stopped.
- Reviewed current signed-DP4A and expanding-load ISA documentation, September 2026 multi-shell lattice serving research and Qwen3-8B ternarisation results. Their packing/performance/quality tradeoffs do not prove a 50 ms limit for this checkpoint. `--scaled-dp4a` is opt-in and retains all 32 codebooks; the goal remains active.

## Native projection scheduling continuation (2026-09-20)

- The preceding scaled-unpacking pass was progress. Revisited the remaining projection bottleneck, preserving all 32 codebooks and the selected calibrated weights. Both original supervisor services remain ready with unchanged PIDs and voice caches; no temporary server was launched in this pass.
- Implemented native warp/register tiling, activation caching, unchanged-value group permutation, shared sum/coefficient staging, independent integer instruction chains, integer-only staging, and explicit Gluon layouts. Added native INT8 tensor-core projections, an offline A-fragment packing and a limited-unrolling version. Across 448 configuration measurements, 444 configurations pass all 18 sampled checks; 8,054 of 8,064 operator checks are exact. Four Gluon variants account for the ten failures and are rejected.
- Fewer barriers do not produce a general speedup. Native warp scheduling and tensor-core paths remain slower. MMA repacking improves that implementation substantially but still loses to DP4A. Independent register-mapping verification passes 401,408 integer dots and packing roundtrip checks under private-stream CUDA graph execution; memcheck reports zero errors. Initial scale-format and Gluon compilation failures remain documented and excluded from the completed-sweep counts.
- One staged native output projection improves 5.1067 to 4.9993 microseconds in twelve rotating/reversing ring rounds, median paired gain 2.24%. Its isolated R4/W8 implementation uses 32 registers, no spills and 4,128 shared bytes. All 504 all-layer real/zero/spike cases and eight padded/actual-row private-stream graph checks match exactly. Memcheck reports zero errors; racecheck reports zero hazards.
- Ten complete, same-process cloned-voice streaming pairs measure 87.481 ms control / 87.444 ms native-output median, but the median paired gain is only 0.0023 ms with five improvements and five regressions. All complete PCM streams match and no request truncates. A 2.96 ms paired outlier is retained with cause unknown. The candidate is not promoted, and no HTTP or new bilingual-suite result is claimed for it.
- Selected scaled DP4A remains unchanged; its prior 87.67 ms improvement result and quality evidence still apply. `REPORT_NATIVE_PROJECTIONS.md` records implementation, complete results, source snapshots and reproduction. The target remains active and unmet. Future work can test larger fusions, remaining normalization/prefill costs or calibrated group-size alternatives; this pass does not establish an absolute latency bound or justify changing the codebook count.

## Primary sources informing experiments (continued)

- [vLLM quantization support](https://docs.vllm.ai/en/latest/features/quantization/): Hopper supports Marlin. Test packed-weight native kernels instead of inferring performance from nominal bit width.
- [Marlin authors' implementation](https://github.com/IST-DASLab/marlin) and [paper](https://arxiv.org/abs/2408.11743): CUDA tiling, packed-weight layouts and overlap of dequantization with tensor-core work motivate native-kernel tests.
- [2026 batch-one decode study](https://arxiv.org/abs/2605.30571): measured latency depends on runtime support and launch costs as well as weight traffic. Its results on other GPUs/models do not establish a lower bound for this task.
- [Mirage Persistent Kernel](https://arxiv.org/abs/2512.22219) and [implementation](https://github.com/mirage-project/mirage): evaluate persistent task scheduling and fused operators against the already-graphed path, not against an eager baseline alone.
- [Ada-MK](https://arxiv.org/abs/2605.11581): shared-memory constraints and K splitting motivate inspection of fusion occupancy and synchronization costs.
- [SGLang-Omni TTS documentation](https://sgl-project.github.io/sglang-omni/basic_usage/tts.html) and [MOSS feedback graph issue](https://github.com/sgl-project/sglang-omni/issues/1707): full pipeline topology and eager work outside graphs affect measured speech latency. Our custom path already captures feedback embeddings; verify the current engine behavior separately.
- [MOSS-TTS technical report](https://arxiv.org/abs/2603.18090): preserve the checkpoint's delay dependencies and distinguish codec bitrate changes from an architecture substitution.
- [Triton 3.8 release notes](https://github.com/triton-lang/triton/releases/tag/v3.8.0) and [September 2026 bitwise GPU-kernel study](https://arxiv.org/abs/2609.11356): inspect reduction order, FMA placement and layout lowering when compiler changes alter speech output. Measurements in this pass use our checkpoint and hardware, not the papers' performance claims.


## Normalization/consumer fusion continuation (2026-09-20)

- The preceding native-projection sweep was progress despite rejecting its candidate. Kept all 32 codebooks and both original services. Fused residual/RMSNorm/G32 quantization into QKV and paired gate/up/SiLU/output quantization using explicit Gluon layouts and register-fed inline DP4A. All 20 layout configurations pass 180 frozen-input checks; selected combined operators improve 8.657 → 8.213 µs and 20.660 → 19.639 µs.
- Captured 3,168 real inputs across every normalization site and four held-out calibration utterances. With edge inputs, all 3,312 private-stream cases match exactly. Twelve private-graph memory cases pass memcheck/racecheck with zero errors/hazards. Neither selected kernel spills. Sixteen complete-graph boundary/fallback checks match every text/audio logit.
- Ten alternating streaming pairs improve 87.25 → 85.48 ms median, with 1.77 ms median paired gain and every pair faster. All PCM matches the prior calibrated path. An outlying 5.57 ms pair remains unattributed. Separate five-request benchmark: 85.61 ms / p95 86.06; diagnostic metrics and saved WAV exact. All 16 bilingual generated WAVs and four references match byte for byte, retaining the small-suite 5.37% CER / 0% WER / 0.9143 speaker-cosine evidence without rerunning ASR.
- Sequential HTTP comparison improves 92.44 → 88.89 ms median, p95 95.95 → 90.83. All 20 complete streams match; no truncation; error/cancellation/recovery checks pass. Fresh-reference single observations: 156.25 / 127.75 ms. Stage timings show 2.234 → 2.170 ms initial decode; prefill and codec are similar. Preparation and scheduling also vary, so the whole HTTP difference is not attributed to fusion. Both temporary servers stopped.
- Initial fused-model warmup exposed a missing local import; fixed before measurement, with failed log retained. Default BF16 regressions match their prior diagnostics/WAVs separately under Torch 2.13 (166.54 ms) and Torch 2.9 (171.06 ms). An initial cross-runtime comparison failed and was corrected to matched-runtime controls.
- The profile removes 2,376 standalone normalization launches. Fused consumers plus remaining projections consume 58.98% of GPU time and remain the main bottleneck. Added opt-in `--norm-projection` to benchmark, quality and HTTP entry points. `REPORT_NORM_PROJECTION.md` records measurements, qualification and reproduction. The 50 ms goal remains active and unmet; no evidence justifies reducing codebooks.

- Continued into masked output-head work. An exhaustive prefix-size pilot covers 416 input/size comparisons; six smaller shapes change active logits and are rejected. Prefixes 8/16/24/32 preserve the tested arithmetic and full sampling shape. They omit only future channels that the original delay mask already discards; every emitted codec frame still uses all 32 codebooks.
- Added an opt-in graph manager crossing those prefixes with attention capacities. All 1,120 full-model private-stream position/audio-length/delay checks preserve active logits and sampled IDs. Ten alternating complete pairs preserve all PCM and measure 85.27 → 84.84 ms median, median paired gain 0.509 ms, eight improvements/two regressions, and slightly worse p95. All 16 bilingual WAVs and four references remain byte-identical.
- Head-prefix HTTP control/candidate measure 91.01 / 88.72 ms median, p95 93.85 / 93.33; all 20 streams match and cancellation/recovery passes. Candidate has a 139.64 ms outlier and a late speed shift in unchanged prefill/codec. The whole HTTP difference is not a kernel-gain claim. Fresh-reference single observations: 131.64 / 127.32 ms. Both temporary endpoints stopped; original services unchanged. `REPORT_AUDIO_HEADS.md` records this optional, smaller and noisier optimization. The goal remains active with all 32 codebooks.
- Separate head-graph profiling confirms twelve extra graphs cost 384.8 MiB live allocation / 408 MiB reserved and 0.58 seconds of additional warmup after the baseline is ready. Backbone projections remain 59.49% of GPU time; their arithmetic and weight traffic are the next principal targets. This profile pass preserves PCM in its two diagnostic pairs and supplies no replacement for the ten-pair latency result.

## G128 calibration and kernel continuation (2026-09-20)

- The preceding norm/head work was progress. This pass preserves all 32 codebooks and focuses on remaining projection traffic/arithmetic. G128 means weight groups, never audio codebook count. Added packed scaled-integer DP4A with activation groups 32/128, attention-reduction quantization, and producer/consumer fusions. Across two compiler versions, 174 projection configurations cover 3,132 checks (2,353 exact); another 64 producer configurations cover 576 checks. Every candidate meets the pilot RMS threshold, but none is described as an exact replacement for G32. A128 producer fusion is slower and rejected. Twenty private-stream CUDA graph cases pass memcheck/racecheck with zero errors/hazards under Triton 3.7.
- Full G128 GPTQ calibration improves 140/144 projections over round-to-nearest, but its median held-out MSE is 1.536 times G32. Added a 33-point BF16 scale clipping search. Plain unweighted MSE fails badly on sensitive pilot projections and is dropped. Training-activation-weighted search improves 140/144 projections versus max-scale G128; median MSE ratio 0.8514, still 1.3000 times G32. Twelve independent scale checks and two GPTQ feedback comparisons pass; memcheck reports zero errors. An initial Torch reference rounding mismatch was traced to Python-scalar division, corrected with tensor division, and documented with the failed fixture; the Triton search kernel was unchanged.
- Full G128 optimization proceeds from 101.74 ms original-layout TTFA through 89.04 packed, 87.06 attention-fused A128, and 84.33 fully fused A32. Isolated Triton 3.8 reaches 83.15 ms with max scales, or 83.39 ms with weighted scales. These five-request rows are separate processes, not paired kernel-gain estimates; all include BF16 prefill, FP32 codec and cached cloned-voice conditioning. The compiler-only environment is not a supported vLLM/Inductor stack. No G128 HTTP/fresh-reference latency is claimed.
- Original 16-sample normalized CER: max-scale A128 5.83%, max-scale A32 9.63%, weighted A128 5.37%, weighted A32 7.39%, weighted A32/Triton 3.8 3.80%. WER is zero throughout; intended-reference ranking and finite/nontruncated checks pass. Speaker cosine varies 0.9115–0.9177. An earlier comparison of raw 19.01% G128 CER against normalized 5.37% G32 CER was invalid and explicitly corrected; both raw and normalized artifacts remain available.
- Froze sixteen additional bilingual prompts before evaluating BF16, selected G32 and weighted G128/Triton 3.8 with four references (32 utterances each). All 96 finish and rank the intended reference first. Normalized Chinese CER is 2.79%, 1.18%, 4.89%; English WER 0%, 0%, 0.30%; speaker cosine 0.9199, 0.9149, 0.9270. Candidate `zh0_6` has a spurious tail in its ASR transcript. Despite favorable reused-suite scores, the full G128 candidate is not selected. This remains a small machine-scored diagnostic set, not unseen-speaker/human acceptance.
- Tested a narrower mixed-weight preset: retain G32 QKV/up and producer fusions, use weighted G128 only for all 72 output/down projections, retain Triton 3.7 and G32 activations. Separate median is 84.01 ms. Original/additional Chinese CER is 7.86%/1.96% versus G32 5.37%/1.18%; WER remains zero, all 48 samples finite/nontruncated/intended-reference top-one. This candidate is also not selected. The additional texts are reused in this follow-up and are no longer untouched selection data.
- Ten same-process alternating mixed-weight pairs measure 84.84 → 83.76 ms median, p95 85.19 → 84.50, **1.013 ms median paired gain**, every pair faster. Both paths use head-prefix graphs and preserve all 32 codebooks; the experiment retains separate quantized buffers and restores eager paths as well as graphs. Forty-eight private-stream eager/graph checks pass exactly. The fixed-seed G32 control WAV matches the prior artifact. Different-model PCM equality is not claimed. This isolates a useful hardware gain but does not overcome the quality regression.
- The selected G32 post-change benchmark retains its exact prior 36-step diagnostic dictionary and saved WAV at 85.52 ms without head prefixes; the paired control above measures 84.84 ms with them. The new full-G128 profile still assigns 58.61% of GPU time to fused consumers and remaining projections. `REPORT_GROUP128.md` records kernels, failed trials, quality corrections and reproduction. Source archiving now includes the extensionless GPTQ license omitted by the prior suffix filter. Existing services remain unchanged. The 50 ms goal stays active and unmet; further weight-traffic/fusion work must continue with all 32 codebooks and independent quality checks.

## Draft acceptance and CUDA load-policy continuation (2026-09-20)

- The preceding G128 pass was progress: kernel/calibration changes and expanded evaluation ruled out candidates that regressed diagnostics. Revalidated the worktree and idle GPU, retaining the selected G32 path, all 32 codebooks and original services. Reviewed recent speech/approximate/carryover speculation research and current Triton/PTX load documentation, then measured concrete alternatives.
- Added a joint-distribution draft audit on eight target-generated first-frame histories from four cloned voices. Six untrained layer-skipping drafts use independent caches and their own prefill, followed by teacher forcing on target history. The full 36-layer control reproduces all 256 probability tensors exactly; 1,792 layout/context comparisons are saved. Every active codebook and unforced text slot contributes to joint acceptance. The 34-layer draft costs 2.025 versus 2.130 ms and has only 8.46% mean joint acceptance; 12–24-layer variants are cheaper but have less than 0.5% mean acceptance. No accepted proposals are observed in the late 34-layer Monte Carlo samples, which is not proof of zero probability. A deliberately optimistic one-proposal cost proxy still predicts slowdowns. These measurements reject the tested drafts, not trained speculation or all possible layer selections; no speculative serving path or absolute latency bound is claimed.
- Added experimental cache operators, L1 eviction priority, PTX L2 prefetch, CTA ordering and register caps for fused G32 projection kernels. Two sweeps attempt 152 configurations: sixteen unsupported cache/eviction combinations fail and are now guarded; 1,224 numerical checks complete, with 1,134 exact. Ninety first-sweep failures expose an experimental output-scale index that was not remapped with CTA order. Fixed it; the second sweep passes every compiled candidate check. Prefetch and tight register caps are slower; corrected gate/up reordering has no useful gain.
- QKV early weight eviction plus L2 scale loads initially improves 8.204 → 7.385 microseconds; twelve rotating/reversing rounds reproduce 8.139 → 7.392. It passes 1,656 all-layer input checks exactly and twelve padded/private CUDA graph cases under memcheck/racecheck with zero errors/hazards. However, the timing ring reuses a single layer's normalization buffers. Rotating all 36 actual layers and input/residual/norm buffers instead gives 8.688 → 8.658 microseconds, just 0.034 microseconds median paired gain. Future producer-fusion screening must distinguish these traffic patterns.
- Ten complete streaming pairs give only 0.110 ms median gain (six improve/four regress). Follow-up twenty pairs give 0.134 ms (twelve improve/eight regress), medians 84.66/84.59 ms. Twelve alternating full decode graph timings show only 0.00366 ms median GPU gain. All thirty paired complete PCM streams match; both runs also pass 48 private full-graph boundary/head-prefix comparisons. A late speed shift affects both paths and unchanged prefill/codec; all samples remain in primary results, and the 79.95 ms minimum is not a promoted optimization result.
- The five-request benchmark measures 86.77 ms without head prefixes, with the exact preceding 36-step diagnostic dictionary and WAV. All sixteen original-suite and thirty-two additional-suite WAVs plus eight reference files remain byte-identical; existing matched ASR/speaker scores apply without rerunning evaluation. No quality regression is observed, but the latency evidence does not justify serving selection. Retained `--qkv-load-policy` only in benchmark/quality tools; removed the unrun HTTP harness and restored `server.py` byte for byte to its previous snapshot. No HTTP or fresh-reference result is claimed for this pass.
- `REPORT_DRAFT_AND_LOAD.md` records the evidence, source archive, numerical limits and reproduction. The selected streaming/cloning path and both supervisor services remain unchanged. Fused consumers plus projections remain approximately 59.5% of profiled GPU time. The 50 ms goal stays active and unmet; there is no proof supporting a codebook reduction. Larger arithmetic/traffic improvements and trained joint-acoustic drafts remain separate next avenues.
## Lossless CUDA allocation continuation (2026-09-20)

- The previous draft/load pass was progress, not a blocking condition. This pass keeps all 32 codebooks and tests H200 inline memory compression through native C++/CUDA Driver API VMM allocation, with DLPack ownership and explicit per-allocation property checks. No driver, global allocator, service or checkpoint replacement occurs. The SM90 wrapper allocates before graph capture and reports external memory separately from Torch's allocator.
- Forty-eight random-bit copy/private-graph/view-lifetime cases pass. An initial sanitizer-side immediate-cleanup assertion is corrected by explicitly releasing the final view and waiting for CUDA before checking counters; the allocator code is unchanged. Final memcheck: zero errors, zero live allocations and zero cleanup errors. Initial failure log is retained.
- Five allocation variants rotate all 36 actual layer buffers per projection. All 576 checks are exact. Packed weights and fused QKV/gate-up do not benefit. FP32 output/down scale compression gives small isolated gains; the 72 selected buffers occupy 288 MiB logically and physically. All 687 codec parameter/table buffers preserve 66 frames but slow graph decode 4.778 → 4.871 ms and increase VMM footprint from 3.61 GB logical to 4.83 GB allocated. Audio-head tests pass 96 checks but show mixed/slower timings. Broad compression is rejected.
- Two independent fifteen-round, three-way full-stream comparisons preserve all PCM and pass 192 combined private-graph logit/ID checks. Compressed scale candidate medians are 84.430 / 84.329 ms, with median paired gains 0.206 / 0.151 ms and 14/15 / 9/15 faster rounds. Plain VMM has no gain. Repeated complete GPU graphs improve 6.917 / 5.760 microseconds per decode. Request noise and slightly worse repeat p95 are retained, not hidden. All 48 cloned-voice suite WAVs and eight reference files remain byte-identical to G32; no ASR rerun is needed for identical files.
- Added optional `--compressed-scales` to benchmark, quality and server. The independent benchmark without head prefixes gives 85.264 ms median and matches the prior 36-step diagnostics/WAV. Sequential HTTP medians are 88.203 → 87.974 ms, p95 90.582 → 88.969; all twenty complete streams match and both cancellation tests recover with 429 then 200. Fresh-reference single observations are 134.97 / 127.64 ms and are not a repeatable speedup claim. Both temporary endpoints are stopped; original supervisor PIDs remain 54030 and 56179.
- Profiling still assigns 59.42% of GPU time to fused and remaining backbone projections. The 50 ms goal remains active and unmet; no evidence establishes impossibility and no codebook reduction is authorized by this pass. `REPORT_COMPRESSION.md` records the new implementation, exactness scope, rejected allocations and reproduction. Next work should continue on the dominant projection cost rather than extrapolate this small bandwidth gain.

## INT4 PTX and exact scale-storage continuation (2026-09-20)

- The preceding compression pass was progress. This pass retains all 32 codebooks, original services, streaming voice cloning, BF16 prefill and the FP32 codec. Implemented exact signed-INT8 activation decomposition into unsigned-low/signed-high nibbles for M8/M16 INT4 PTX MMA. Both shapes pass 4,816,896 independent integer-dot checks and memcheck with zero errors. Thirty-two actual-layer projection configurations pass all 6,912 output comparisons; twenty-eight padded/private-graph cases also pass memcheck. An initial Python dtype-view stride failure is corrected and its log retained.
- The INT4-PTX kernels are substantially slower: best QKV/output/up/down times are 26.53/19.84/92.24/54.02 microseconds versus selected DP4A 5.95/4.94/16.89/9.94. Native SM90 SASS shows expansion into INT8 IMMA plus conversion sequences. Earlier shorthand calling these native INT4 was inaccurate and has been corrected. No full-model or HTTP gain is claimed; the candidate is rejected. `REPORT_INT4_PTX.md` records the code and audit, including the distinction between ISA format support and useful hardware throughput.
- Revisited exact BF16 storage for the 72 output/down scale buffers. Their exported values are already BF16-representable, but implicit compiler layouts can change reduction order. Explicit Gluon layouts give a small exact gain while shrinking storage from 288 to 144 MiB. Thirty-six BF16 pilot configurations plus four family controls yield 684 comparisons, 664 exact; failed variants are rejected. Selected output/down medians are 4.857/9.760 microseconds, versus ordinary FP32 4.955/10.031 and compressed FP32 4.930/9.853. All 1,296 expanded all-layer checks and sixteen private-graph cases pass; memcheck and racecheck report zero errors/hazards.
- Two independent fifteen-round three-way comparisons measure BF16 candidate medians 84.192/84.127 ms, with 0.524/0.441 ms median paired gains over ordinary FP32 (15/15 and 14/15 faster). Gains over compressed FP32 are 0.322/0.201 ms. Candidate p95 slightly exceeds compressed FP32 p95, although it improves over ordinary storage. All ninety measured complete PCM streams and 192 private full-graph logit/ID comparisons are exact. Repeated whole-decode graphs gain 9.160/9.293 microseconds. Every sample and outlier remains in the artifacts.
- All 48 cloning-suite WAVs, generation metadata and eight reference files are byte-identical to selected G32; prior normalized CER 5.37%/1.18% and WER 0%/0% apply without ASR reruns. Independent benchmark CLI validation reproduces the complete previous 36-step diagnostic dictionary and WAV, measuring 85.281 ms median / 88.469 ms p95 without head prefixes. These are equivalence checks to calibrated G32, not unquantized BF16 or broad quality acceptance.
- Added optional `--short-scales` to benchmark, quality and server, mutually exclusive with compressed FP32 scales. Sequential compressed-FP32/BF16 HTTP tests measure 88.643/87.988 ms median, p95 91.358/88.225; all twenty complete streams match and cancellation/recovery returns 429 then 200. Internal engine medians are 84.675/84.253 ms. Control warmup/scheduling variation prevents assigning the entire HTTP difference to the kernel. Fresh-registration-plus-synthesis single observations are 130.77/129.13 ms; registration itself is 36.37/37.40 ms. Both temporary endpoints are stopped; original services remain unchanged.
- Saved actual selected cubins/PTX/IR/SASS. Output retains 2,048 shared bytes, changes 30 to 32 registers and five to three static barriers; down retains 128 registers, 1,024 shared bytes and twenty barriers. Neither spills. Static counts are not measured dynamic traffic. Updated profiling still places 59.25% of GPU time in fused consumers and projections. `REPORT_SHORT_SCALES.md` records qualification and reproduction; source snapshots include this pass and the rejected INT4-PTX work. The 50 ms goal remains active and unmet with all 32 codebooks. Larger projection improvements remain necessary.

## Programmatic projection dependency continuation (2026-09-20)

- The preceding exact scale-storage pass was progress. Revalidated the authoritative worktree and idle H200; original service PIDs and all 32 codebooks remain unchanged. Reviewed current CUDA/Triton PDL documentation, native disassembly reference, recent matched PDL/megakernel engineering evidence and August Blackwell decode notes. Implemented exact Gluon projections with programmatic launch attributes, explicit grid waits and varied trigger/preload placement. This preserves calibrated G32, BF16 prefill, FP32 codec and streaming cloning.
- Twelve trigger configurations and sixteen preload combinations are screened on 36 actual-layer dependency chains. The trigger sweep passes 1,404 comparisons and improves 37.967 to 36.208 microseconds. The preload sweep passes 1,944 comparisons; scale-only output/down preload reaches 36.060 versus its 37.873 control. Full weight preloads regress to 54.57–66.88 microseconds and are rejected. Adjacent ring chains use saved inputs, not a complete attention stack, so these results are screening evidence. Private-stream captured graphs retain the expected CUDA programmatic edge types, verified through cuGraphGetEdges_v2.
- Thirty-six changing-input/private-graph chain cases pass memcheck with zero errors and racecheck with zero errors/warnings. Selected SM90 QKV/up/output/down resource counts remain 56/162/32/128 registers, 2048/1024/2048/1024 shared bytes and zero spills. Whole-matrix gate-up preload reaches 255 registers and n_spills=212. Saved PTX/SASS confirms immutable scale loads before the wait and dependent loads after it. An initial audit parser looking for PTX names in SASS is corrected to ACQBULK/PREEXIT from the preserved files; no timing or kernel changes result.
- Two independent fifteen-round complete-stream comparisons measure selected medians 83.461/83.466 ms with median paired gains 0.757/0.639 ms over ordinary short scales. All thirty selected rounds are faster; basic PDL gains 0.387/0.324 ms. All ninety measured complete PCM streams and 192 private full-graph logit/ID checks are exact. Candidate p95 is 84.597/83.984 ms, versus controls 85.403/89.411; outliers remain and the full p95 difference is not attributed to the kernel. Complete decode graphs gain 17.13/16.66 microseconds per replay. The repeat validates integrated per-model configuration instead of the first harness's temporary global function replacements.
- All 48 cloned-voice WAVs, generation metadata and eight reference WAVs remain byte-identical to the preceding short-scale path. Previous normalized CER 5.37%/1.18% and WER 0%/0% apply without ASR reruns; these reused four-voice diagnostics do not establish broad human quality acceptance. Independent benchmark CLI validation reproduces the entire prior 36-step diagnostic dictionary and WAV at 84.605 ms median / 84.762 ms p95 without head prefixes.
- Optional --projection-pdl requires exact short scales and remains off by default. Sequential HTTP medians are 88.304/87.586 ms, p95 90.327/87.921, with all twenty PCM streams/frame counts identical and cancellation/recovery 429 then 200. Internal engine medians are 84.418/83.768 ms; prefill and first codec stages are virtually unchanged. Fresh-reference single observations are 130.74/129.81 ms, registration 38.81/38.57; no latency-distribution claim follows from those singles. Both temporary servers stop, and original services remain unchanged.
- Profiler interpretation changes under PDL: waits are inside resident kernel intervals and overlaps double-count summed durations. Three profiled chains have 167.84 microseconds summed / 111.65 union for basic PDL and 162.85 / 109.54 for selected preload, versus ordinary 114.24 / 114.24. The new full-stream projection percentage is not comparable with the earlier serialized 59.25% breakdown. Graph events and complete-request timings establish the gain. REPORT_PROJECTION_PDL.md records the implementation, qualification, rejected variants and reproduction. The goal remains active and unmet at 50 ms; nothing establishes impossibility or permits a codebook reduction. Further projection arithmetic/traffic work and remaining ordinary dependency boundaries are the next targets.

## Attention dependency overlap continuation (2026-09-20)

- The preceding projection-PDL pass was progress. Extended compatible CUDA programmatic dependencies through QK normalization/RoPE/cache writes, native attention and reduction/G32 quantization. All 32 codebooks, calibrated weights, BF16 prefill, FP32 codec and streaming voice cloning remain intact. Current CUTLASS/CUDA guidance informs synchronization and launch attributes; no third-party kernel body is copied.
- Twenty-five five-kernel configurations pass 2,592 all-layer comparisons, plus 72 pilot comparisons. The 36-layer ring uses actual weights and norm inputs with representative frozen KV fixtures. Ordinary control is 22.848 microseconds, selected q1/a2/r1 is 19.626, and a Q/K norm-preload candidate is 19.568. Attention-only PDL regresses to 23.520. CUDA graph edges are inspected. Both memcheck and racecheck pass 168 changing-input, guarded-cache, poisoned-future and context-boundary cases with zero reported errors/hazards.
- Three fifteen-triplet full-model comparisons preserve all 135 measured float32 PCM streams and pass 288 full-graph logits/ID checks. Selected median TTFA is 79.707 / 77.394 / 77.497 ms; median paired gains are 4.002 / 3.612 / 1.860 ms. The second run changes runtime phase in unchanged prefill/codec work; all observations remain. The third run confirms improvement in the later phase, with one slower outlier retained (14/15 faster). No claim attributes the phase shift to this code. Q/K preloads add no consistent gain across phases, so the simpler configuration is selected.
- All 48 original/additional bilingual cloning WAVs, generation metadata and eight references match the prior qualified projection-PDL path exactly. Existing normalized CER/WER and speaker diagnostics apply to identical files; no new ASR run or broader quality acceptance is claimed. Independent CLI validation preserves its complete 36-step diagnostic dictionary and saved WAV, measuring 80.538 ms median without audio-head buckets.
- Sequential temporary HTTP servers measure 87.353 to 83.613 ms median, with all twenty complete PCM streams/frame counts exact, validation passing and cancellation/recovery returning 429 then 200. Engine medians are 83.812 / 79.841 ms; initial decode mean 2.1276 / 2.0039 ms, with unchanged prefill and codec timings. A single fresh-reference registration plus synthesis observation is 124.764 ms; registration alone 37.388 ms. Both temporary servers are stopped; original supervisor processes remain unchanged.
- SASS/resource audit retains 30 QK registers and 40 native-attention registers, with no spills. Native ptxas reports 2080 bytes of user shared memory; its separate cuobjdump resource report prints 3104, and both are preserved. PDL durations overlap and include waits. The selected full-request interval sweep still finds projection-only residency dominant (39.17 ms versus 6.18 ms attention-only, plus 10.68 ms overlap), not an arithmetic-utilization measurement. Projections remain the next main target; codec cost remains about 4.4–4.8 ms.
- An initial paired launch was aborted before timing because racecheck had not yet exited; its log is retained and completed latency work runs sequentially. An HTTP method-label error naming the unselected preload option is corrected with the original preserved; measured outputs and timings are untouched. `REPORT_ATTENTION_PDL.md` records evidence, limits and reproduction. Optional `--attention-pdl` requires the qualified projection/native/quantized-attention preset. The goal remains active and 50 ms unmet; these measurements do not justify changing the 32-codebook value.

## Shared codec clock continuation (2026-09-20)

- The preceding attention-dependency pass was progress. Retained all 32 codebooks, FP32 codec arithmetic, streaming voice cloning and both original supervisor services. Replaced 68 per-attention offsets and unused transformer bookkeeping with four immutable-per-frame stage clocks, advanced together after the complete decoder frame. Specialized first-frame single-token attention to return V while preserving K/V cache writes and the original QKV projection. Each wrapper owns its cache state and both graphs; first and steady graphs share that state, while control and candidate wrappers in the paired test retain independent buffers. Reset clears only authoritative clocks.
- The clean 140-frame screen spans every KV-cache wrap and all-zero/all-1023/random first-frame probes. Selected output is exact; median first frame changes 4.776 to 4.540 ms. Shortening first-frame reduction shapes and omitting unused Q projection both change floating output and are rejected. Initial comparisons initialized profiling between variants and therefore cannot establish timing gains; caveats and source snapshots are retained. Corrected timing finishes before any profiling.
- Dedicated private-stream validation compares 147 frames per path, all 68 KV buffers as bit patterns, NaN-poisoned future slots, repeated resets and the last valid rotary position. All outputs/caches are exact and the next frame is rejected before launch. Eleven-frame memcheck reports zero errors; filtered racecheck of changed/original rotary-cache kernels reports zero hazards/errors/warnings. An unfiltered racecheck was terminated while instrumenting unchanged library work and is explicitly not a completed whole-codec check.
- Twenty alternating complete-stream pairs improve 79.803 to 79.535 ms median, with 0.301 ms median paired gain and 17/20 faster. All forty measured FP32 PCM streams match exactly. Prefill and LLM timings are unchanged; first codec median changes 4.769 to 4.517 ms. All 48 bilingual cloning WAVs, metadata and eight reference files match the preceding selected path. Prior normalized machine-scored diagnostics apply to identical audio; ASR is not rerun and broad human acceptance is not claimed.
- Sequential temporary HTTP medians are 84.971 / 83.062 ms, with twenty exact complete streams and successful malformed-input/cancellation/recovery checks. Unchanged host/LLM stages also vary, so the whole 1.91 ms difference is not a codec-gain claim. One fresh registration plus synthesis observation takes 122.293 ms; registration alone 35.596 ms. Both temporary endpoints stop. Independent benchmark CLI without head buckets measures 80.223 ms median / 80.315 ms p95 and preserves the complete prior 36-step diagnostic dictionary and WAV.
- Revisited projection partitioning under PDL with twenty configurations: 1,968/2,052 chain comparisons are exact; two R8 QKV configurations account for all 84 failures and are rejected. Splitting gate/up is slower. A split R4 QKV configuration has only four positive paired rounds of six and no convincing gain, so no new LLM path is selected.
- Selected codec graphs remove 111 kernel launches from the first frame and 79 from steady frames. The complete diagnostic profile still finds projections dominant: 39.881 ms projection-only resident intervals, 9.516 ms projection/attention overlap, and 6.760 ms attention-only. PDL intervals include waits and are not utilization or critical-path percentages. Optional `--codec-clock` is wired into benchmark, quality and server entry points. `REPORT_CODEC_CLOCK.md` records qualification and rejected alternatives. The goal remains active and 50 ms unmet; no codebook reduction is justified. Larger projection arithmetic/traffic and dependency-scheduling improvements remain necessary.

## Projection resources and warp partition continuation (2026-09-20)

- The preceding codec pass was progress. Revalidated the idle H200, original two service processes and prior source snapshot. Kept all 32 codebooks and the qualified streaming cloning path. Reviewed current CUDA shared-spill/PDL documentation, Gluon warp specialization, the 2026 compiler roadmap and Twill scheduling research. These sources motivate experiments; their attention/tensor-core results are not evidence for this model's DP4A workload.
- Extracted a pinned, SHA-256-verified CUDA 13.0.88 assembler into an isolated directory, without changing Python packages, the system toolkit or driver. Added an experimental CUDA Driver API launcher retaining the caller's stream and PDL attributes. Reassembled original Triton PTX with optional static shared declarations, register caps and shared spilling; original/transformed PTX, cubins, logs and hashes remain available.
- Twenty-nine resource configurations pass 3,161 real-layer chain/private-graph comparisons. The six-round 36-layer ring measures 35.988 microseconds for the control, 36.036 for CUDA 13 dynamic shared and 36.056 for static shared. Gate/up capped at 128 registers increases theoretical CTA occupancy but slows to 44.269 with local spills or 43.082 with shared spills. Down at 112 with shared spills removes its local allocation and reaches 36.132, still slower than control. No resource alternative is selected; theoretical occupancy is not measured concurrency.
- Implemented two parallel warp layouts: gate/up branches in separate four-warp partitions, or half the output rows per partition. Both preserve four-warp normalization order and synchronize shared results with an mbarrier. Sixteen variants plus a control pass 1,853 comparisons. Best row partition is 39.657 microseconds and best branch partition 43.424, versus their 36.089 control. Initial constexpr-passing and unsupported transpose-alias compile failures are corrected, with original sources/logs retained. The final sweep has no errors or mismatches.
- Across both completed sweeps, 44 alternatives plus two controls produce 5,014 exact comparisons. Dedicated changing-input/private-graph validation passes 54 cases under each sanitizer; memcheck reports zero errors and racecheck zero hazards/errors/warnings. These representative checks do not imply whole-serving qualification. No candidate improves the operator chain, so there is no full-request speedup claim, serving promotion or new ASR run.
- The qualified 79.53 ms warm cached-voice result remains unchanged; 50 ms is still unmet. The next concrete scheduling target is the CPU readback and host state update after each of the first 32 audio decode steps. A bounded GPU graph may remove those round trips, but its RNG, early-delay, context-boundary, cancellation and complete-stream behavior remain unimplemented/unverified here. `REPORT_PROJECTION_RESOURCES.md` records this evidence and the rejected candidates. Nothing proves the target impossible or justifies reducing codebooks.


## Initial audio CUDA graph continuation (2026-09-20)

- The preceding resource/warp audit was progress through measured rejections. Implemented its proposed bounded scheduling change: the 32 complete audio steps after `audio_start` execute inside one CUDA graph, with a Triton device-state/history update after each sampler. No step, codebook, weight or sampler draw is omitted. Captures directly record model operations for PyTorch RNG tracking; no nested graph replay is used. Current CUDA/PyTorch capture documentation informed the implementation.
- Four captured context capacities cover 128/256/512/1024, including intervals crossing previous per-step boundaries. The stream emits its first full 32-codebook PCM frame immediately after the interval and resumes ordinary incremental output. Per-step times inside the interval are null; the real aggregate is reported separately. Four captures add approximately 128 MiB live / 136 MiB reserved allocation and 3.13 seconds capture time. The optional `--first-audio-graph` is wired into benchmark, quality generation and server entry points.
- Six pilot intervals match every logit, sampled ID, state and RNG result. Full boundary validation covers 38 intervals / 1,216 steps per path, all four capacities and the final KV position. Every text/audio logit, sampled ID, final RNG/state and all 72 entire KV buffers agree bitwise, including NaN-poisoned future slots on a private stream. Memcheck reports zero errors; racecheck scoped to the changed `_advance` kernel reports zero hazards/errors/warnings. Each sanitizer covers two full 32-step intervals, with boundary coverage supplied by the separate validator.
- Twenty alternating complete-request pairs measure 79.505 → 77.910 ms median warm cached-voice TTFA, p95 79.963 → 78.968 ms, median paired gain 1.616 ms and 20/20 improvements. All 40 measured complete PCM streams and final RNG states match; the twenty control hashes also match the preceding codec-clock pass. Initial 32-step work falls from 63.988 to 62.345 ms; prefill stays 9.217 / 9.216 ms and first codec work 4.519 / 4.520 ms. Profiling begins after all timing.
- All 48 generated cloning WAVs, eight references and metadata match the preceding selected suites byte for byte. Existing normalized Chinese CER 5.37% / 1.18%, English WER 0% / 0%, speaker cosine 0.9143 / 0.9149 apply to identical audio; no redundant ASR rerun or broad quality claim is made. This remains calibrated-G32 equivalence, not original BF16 equivalence.
- Sequential temporary HTTP servers measure 83.116 → 81.538 ms median, p95 83.310 → 82.291 ms, with twenty exact full PCM streams and successful malformed-input/cancellation/recovery checks. One fresh reference registration plus synthesis observation is 122.020 / 120.408 ms; registration alone is 35.338 / 35.473 ms. These fresh results are single observations, not distributions. Both temporary endpoints stop; original supervisor services remain unchanged.
- The archived stream-loop regression compares the pre-change implementation with current disabled/enabled paths for two languages and budgets 33/34/40/400. All eight three-path cases match full PCM, frames, truncation, prompt/step counts and RNG. Too-short/oversized budgets reject cleanly; busy-owner protection, early-close recovery and stable yielded PCM pass for both current paths.
- Independent benchmark CLI without audio-head buckets measures 78.607 ms median / 78.951 ms p95 over five complete requests. The full preceding 36-step diagnostic dictionary and saved WAV match exactly, qualifying the separate full-head capture configuration. This separate-process timing is not another isolated gain estimate.
- The 34-step diagnostic profile reduces graph launch calls from 36 to 5, async copy calls from 151 to 27 and kernel events from 14,060 to 13,937. Projection/attention event counts stay unchanged. Projection-only resident intervals remain 39.822 ms, projection/attention overlap 9.519 ms and attention-only 6.729 ms; PDL intervals include dependency waits and are not utilization percentages. The approximately 62.35-ms initial LLM interval remains the principal target. `REPORT_FIRST_AUDIO_GRAPH.md` records implementation, limits and reproduction. The 50-ms goal remains active and unmet; all 32 codebooks are retained.


## Hopper bulk-prefetch continuation (2026-09-20)

- The preceding initial-audio graph pass was progress: paired TTFA improved 1.616 ms with exact streaming output. Revalidated all 271 archived source hashes, idle H200 and the two original GPU processes. Kept all 32 codebooks. Reviewed current PTX bulk-prefetch constraints and recent batch-one serving-layout/lossless-compression research; external performance results are not predictions for MOSS.
- Added experimental asynchronous `cp.async.bulk.prefetch.L2.global` hints before existing projection dependency waits. Only immutable weight/scale buffers may be hinted; one CTA thread issues each hint, and original waits/arithmetic remain. Host dispatch copies preserve original validation without changing source globals. Misaligned contiguous buffers reject before launch. The new direct eviction-priority suffix documented for a later GPU family is not used on SM90.
- Screened 53 configurations over all 36 real layer chains: stage/prefix size, scale hints, cache-policy descriptors, CTA lookahead and combined stages. All 5,777 output/intermediate comparisons match; no configuration fails. Original and wrapper-only controls measure 36.025 / 36.031 microseconds. Hinting the first 1/16 span in QKV, gate/up and down measures 35.552, with 0.487-microsecond median paired gain and all six rounds faster. Full-span hints regress to 42.868. Registers/shared memory remain unchanged, with zero spills.
- Two twenty-pair full-request runs measure 77.658 → 77.257 and 77.775 → 77.274 ms median warm cached-voice TTFA. Median paired gains are 0.446 / 0.483 ms, with 18/20 and 19/20 improvements. All 80 measured complete PCM streams and final RNG states agree, and both runs' controls match preceding-pass PCM hashes. Each run adds 48 exact complete-graph context/head checks. Initial 32-step work falls from about 62.3–62.4 to 61.87 ms; prefill and codec costs are stable.
- The dedicated projection validator passes 66 changing-input private-graph cases under both sanitizers, covering actual/odd row counts, complete/prefix hints, cache policy and wrapped lookahead. Eleven misaligned weight buffers reject. Memcheck has zero errors; unfiltered racecheck has zero hazards/errors/warnings. This is projection coverage, not whole-service sanitizer coverage.
- Added opt-in `--bulk-prefetch` through per-model/per-module integration before capture, requiring the selected projection-PDL/short-scale/fusion preset. All 48 generated cloning WAVs, eight reference WAVs and metadata match the preceding initial-audio-graph suites exactly. Existing normalized Chinese CER 5.37% / 1.18%, English WER 0% / 0% and speaker cosine 0.9143 / 0.9149 apply to identical audio. No ASR rerun or broad quality claim is made.
- Temporary loopback HTTP validation measures 81.542 → 80.435 ms median, p95 82.778 → 81.314, with all twenty PCM streams exact and malformed-input/cancellation/recovery checks passing. Internal engine TTFA changes 77.834 → 77.273 ms; the larger HTTP difference includes host variation and is not wholly attributed to the hint. One fresh registration plus synthesis observation is 121.908 / 118.380 ms, with registration alone 35.840 / 33.643 ms. Both temporary servers stop.
- The ordinary benchmark CLI without audio-head buckets completes five integrated requests at 77.988 ms median / 78.192 ms p95. The entire previous 36-step diagnostic dictionary and saved WAV remain exact; this is entry-point validation, not another isolated gain estimate.
- Post-timing profiling retains 13,937 kernel events with 4,752 projection and 3,564 attention events. Projection-only resident intervals are 39.933 ms, projection/attention overlap 9.227 ms and attention-only 6.743 ms. PDL intervals include waits and are not utilization or pure arithmetic percentages. The 61.87-ms initial audio LLM interval remains dominant, with 9.22-ms BF16 prefill another opportunity. The target remains active and unmet by about 27.3 ms; no codebook reduction is justified.
- PTX/cubin/SASS audit confirms actual `UBLKPF.L2` before the original dependency wait, with unchanged registers/shared memory and zero spills. The first opcode filter missed the SM90 spelling; its summary is retained and corrected from the saved binaries. The audit exposes a runtime `rem.u32` in the generic zero-lookahead path, lowered to reciprocal/integer correction instructions. Removing that redundant modulo is the next bounded CUDA experiment; no unmeasured gain is claimed. `REPORT_BULK_PREFETCH.md` records the full pass, rejected alternatives and reproduction. All 221 Python files parse and eight CLI help checks pass; original services remain ready and temporary port 18084 is free.


## BF16 prefill preparation and hint addressing continuation (2026-09-20)

- Retained all 32 codebooks and streaming voice cloning. Removed redundant zero-lookahead modulo only from QKV bulk hints after a sixteen-configuration, 1,744-check all-layer screen. Twenty paired complete requests give 0.188 ms median gain (77.181 → 77.028 ms; 18/20 faster), identical PCM/RNG states, and 48 exact context/head graph comparisons. Both sanitizers pass 66 changed-input cases; gate/up and down addressing alternatives are not selected.
- Fused BF16 prefill Q/K normalization, rotary and KV writes into one Triton launch per layer, preserving BF16 rounding and Q strides. The initial synthetic zero case exposed a signed-zero negation mismatch despite exact actual-model/full-stream outputs. Corrected with a sign-bit flip and retained the failed log. Both unfiltered sanitizers pass all 90 random/zero/spike length/offset graph cases on the corrected source, with no errors/hazards. Sixteen complete prefill lengths through 1023 tokens match every logit and all 72 poisoned KV buffers.
- Corrected full-request comparison: **77.085 → 75.305 ms median**, p95 **78.437 → 76.775 ms**, with **1.787 ms median paired gain**, all twenty pairs faster, and all forty full PCM streams/final RNG states exact. Prefill falls from 9.217 to 7.403 ms while initial audio generation remains about 61.7 ms. Corrected all-layer preparation microbenchmark is 60.219 → 7.234 µs. The initial v1 result is retained but does not qualify the final source.
- Integrated optional `--prefill-qkv` in serving, quality and benchmark entry points; all 48 bilingual cloning WAVs, eight references and generation metadata match the prior selected suites exactly. Existing normalized CER/WER/speaker diagnostics apply to identical audio; no ASR rerun or broad quality guarantee is claimed.
- Sequential HTTP comparison measures **81.725 → 79.066 ms median**, p95 **83.490 → 80.390 ms**, with all twenty full streams exact and cancellation/input-error checks passing. The full HTTP difference includes host/runtime variation. Fresh-reference single observation **119.872 → 123.574 ms** is slower, not an improvement claim. Both temporary servers stop; existing supervisor services remain unchanged.
- The corrected profile removes 540 prefill launches (1,023 → 483). Projection-only resident intervals still total 39.632 ms, plus 9.263 ms projection/attention overlap; these include waits, not utilization. Initial LLM generation remains the largest bottleneck. Exact prefill pointwise fusion and further projection scheduling are concrete follow-ups. `REPORT_PREFILL_QKV.md` records implementation, failed and corrected validation, measured limits and reproduction. The target remains active and unmet; no codebook reduction is justified.


## Prefill pointwise fusion and split-normalization decode continuation (2026-09-20)

- The preceding turn was progress, and the current source/results were revalidated before continuing. Kept all 32 codebooks, both original services and streaming voice cloning. Tested 21 prefill activation/product alternatives: direct exponential, libdevice, and a complete BF16 activation lookup table at seven launch configurations. All 819 real-layer/domain comparisons match; direct block-512/four-warp wins at 4.663 versus 15.584 µs. The lookup table is slower and unselected. All 72 real residual/normalization sites match; fusion improves 5.273 → 3.313 µs per site.
- A four-mode, twenty-round full-request ablation measures control 75.317 ms, activation 74.891 ms, residual 75.224 ms and combined **74.730 ms** median. Median paired gains are 0.444 / 0.188 / **0.644 ms**, with 18/16/18 of twenty rounds faster. All eighty complete PCM streams and final RNG states match; all twenty controls match the preceding selected path. All 48 full-prefill comparisons (three candidate modes × sixteen lengths through 1023) preserve every logit and all 72 poisoned KV buffers.
- Added optional `--prefill-pointwise` before graph capture in serving, quality and CLI paths. Both unfiltered sanitizers pass 36 changed-input/private-stream cases each, including all BF16 gate bit patterns and zero/NaN bits. All 48 bilingual cloning WAVs, eight references and generation metadata match the prior selected suites. Existing normalized ASR and speaker diagnostics apply to identical files; no broad quality or original-BF16 equivalence is claimed.
- HTTP median is **78.926 → 78.438 ms**, p95 **79.458 → 79.268 ms**, with all twenty streams exact and malformed-input/cancellation/recovery checks passing. Fresh-reference single observations are 119.701 / 117.887 ms. Independent CLI completes five requests at 76.353 ms median with identical 36-step diagnostics and WAV; this separate-process result is slower than its predecessor and is not a speedup claim. Temporary servers stop afterward.
- Revisited the larger decode bottleneck with a one-CTA norm/quant producer and PDL projection consumers, preserving exact arithmetic. Ten alternatives plus control pass 1,199 comparisons, but all lose (best QKV split 35.817 versus 35.322 µs). Split gate/up uses 192 registers versus control 162. Six explicit register caps plus controls add 872 exact comparisons; best capped chain 37.176 still loses to 35.378 µs. All 2,071 checks pass, and six representative schedules pass 54 private changing-input cases under each sanitizer. No split variant is selected.
- Compiled audits retain PTX/IR/cubins/SASS for the selected 18-register activation and 55-register residual kernels (zero spills), plus decode controls/caps. The selected profile removes 108 prefill launches (483 → 375); prefill is about 6.80 ms. The initial LLM interval remains 61.75 ms and projection-only resident intervals 39.704 ms, plus 9.288 ms overlapping attention; these include waits, not utilization. `REPORT_PREFILL_POINTWISE.md` records the selected change, rejected schedules and measured limits. The target remains active and unmet; no evidence justifies reducing codebooks.

## G64 projection continuation (2026-09-20)

- Retained all 32 acoustic codebooks, streaming and voice cloning. Added G64 weight calibration with activation-diagonal clipping; all 144 exports complete on the same 866 training/410 held-out rows. Six independent scale-search and two GPTQ integer-code comparisons match. Mean held-out projection MSE is 8.3% higher than selected G32. Initial unsupported-group failure is preserved and corrected; original checkpoints remain intact.
- Three 36-layer screens cover 15 G64-weight/G32-activation configurations, 21 wider/mixed activation configurations and 17 selective-weight configurations. Nominal compact-scale kernels use more registers and are slower; the initial BF16 mismatch disproves nominal bit-equivalence. Wider G64 activation groups also lose, with register spills in wide fused gate/up. Factoring shared weight scales helps only selected stages: gate/up + QKV chain 34.989 versus 35.420 µs, median paired gain 0.438 µs.
- Twelve rotating/reversing complete-request rounds measure G32 control **74.779 ms**, G64 gate/up **74.308 ms**, and G64 gate/up + QKV **74.061 ms**. Paired gains are **0.439 / 0.733 ms**, each faster in 11/12 rounds. Separate eager dispatch and context/head/first-audio graphs preserve each model's intended weights. Ninety-six private-stream eager/graph comparisons pass; all twelve controls match prior PCM hashes. Candidate PCM differs and is scored separately.
- Generated 48 bilingual cloning WAVs for each candidate, all finite/complete, with identical prompts, seeds and reference assets. Matched CUDA-FP16 ASR/WavLM scoring gives original/expanded normalized Chinese CER **5.83% / 1.85%** for gate/up and **6.29% / 1.93%** for gate/up + QKV, versus selected **5.37% / 1.18%**. English WER remains zero and all intended-reference top-one checks pass. Both candidates remain unselected because the small speed gains accompany diagnostic regressions; these machine scores are not human listening.
- Selected G32 prefill-pointwise remains the qualified preset. The 61-ms initial LLM interval dominates even the faster experimental models; prefill and codec stay about 6.83 and 4.52 ms. `REPORT_GROUP64.md` preserves timing, calibration, failed trials and quality evidence. No new HTTP or fresh-reference gain is claimed, and no codebook reduction is justified. A useful next hardware experiment is sampled CTA timestamp instrumentation around dependency waits and projection work: current resident intervals include waits, so separating them can guide further exact-arithmetic scheduling without another precision tradeoff.
- Both unfiltered CUDA sanitizers pass 72 changing-input/private-stream cases each, including nominal/factored scales, mixed/wide activation groups, a spilled layout and attention output. Zero memory errors or race hazards/warnings are reported; candidate graph results match their own eager arithmetic. Compiled PTX/IR/cubin/SASS evidence is archived. Original service health still reports all 32 codebooks and the existing two cached voices each; no new service is deployed.


## Projection wait probes and output-weight staging continuation (2026-09-20)

- The preceding G64 pass was progress but changed precision was rejected on quality diagnostics. Revalidated its archive and kept all 32 acoustic codebooks, selected G32, streaming voice cloning and both original services. Added sampled globaltimer probes around existing unconditional CUDA dependency waits. Two pilots exposed probe overhead; compact timestamp storage did not remove it. The targeted 14-configuration all-layer screen passes 1,512 bitwise checks and 14 private graphs. Gate/up wait-only probes add 0.003 µs per synthetic chain; down probes add 1.112 µs and raise registers from 128 to 167.
- Instrumented the actual first-32-step model graph because the isolated ring's gate/up producer differs from the backbone. Full-model gate/up and QKV sampled waits have zero medians and 32-ns p95s; QKV post-wait lane intervals are 6.368 µs median. Down wait is 0.352 µs median, 1.280 µs p95 and 18.112 µs maximum, with intrusive 1.382-ms graph overhead. Four complete cache/state comparisons and eight rounds of IDs/logits/status/RNG equality pass. The final timestamp is lane-zero store issue, not grid completion; SASS clocks execute beyond sampled stores. These are diagnostic intervals, not utilization or a latency lower bound.
- Tested cooperative cp.async, full/compact/partial Hopper TMA, shared-memory swizzles, requested cache modifiers and tile sizes. The 37-configuration down screen passes 3,996 bit checks but every candidate loses; best partial TMA is 36.201 versus 35.409 µs. The 19-configuration standalone output screen passes 4,104 comparisons; all candidates are slower in isolation. Including actual attention producer schedules reveals a shared-copy overlap benefit: seven configurations pass 756 checks, best paired gain 0.422 µs. Frozen attention fixtures are nearest-layer captures, not a full trajectory.
- Shared-copy eight-row full requests gain 0.330 / 0.287 ms in twelve-/twenty-round comparisons, with exact PCM/RNG throughout, but remain experimental after a better schedule is found. A seven-configuration tile/preload ablation adds 756 exact checks. Larger tiles alone lose; ordinary weight loads into registers before PDL wait improve by 0.465 / 0.496 µs for eight/sixteen rows. A twelve-round four-way full-model run gains 0.821 ms for sixteen-row register preload, amid runtime variation.
- The final twenty-pair repeat measures **74.809 → 74.254 ms median warm cached-voice TTFA**, p95 **75.779 → 75.368**, with **0.605 ms median paired gain** and 19/20 improvements. Across all four full-request comparisons, 164 measured complete streams and final RNG states agree, all 64 controls match prior PCM hashes, and 336 control-eager/candidate-graph comparisons pass. Initial LLM interval falls 61.783 → 61.194 ms; prefill/first codec remain about 6.83/4.53 ms. Added mutually exclusive optional `--output-weight-prefetch` (selected register schedule) and `--async-output` (shared-copy experiment) before capture in serving, quality and CLI paths.
- Both candidates generate 48 cloning WAVs each; every generated file, sixteen reference copies and generation metadata match the prior qualified G32 suites. Existing identical-audio Chinese CER 5.37% / 1.18%, English WER zero and speaker cosine 0.9143 / 0.9149 apply; no ASR rerun or human-quality acceptance claim. Both unfiltered sanitizers pass 144 async/probe cases plus 36 register-preload cases, with zero errors/hazards/warnings. Compiled audits retain twelve final kernels: selected register preload uses 96 registers, 2 KiB shared memory and zero spills, with weights/scales before ACQBULK and activations after it.
- Initial shared-copy HTTP median regresses 78.939 → 79.390 ms, with concurrent CPU CLI checks noted as a confound. Final register HTTP comparison runs without concurrent qualification jobs: **80.201 → 79.348 ms median**, all twenty streams exact, malformed-input and cancellation/recovery checks pass. It remains slower than the earlier separate 78.438-ms HTTP run; no cross-session improvement is claimed. Fresh-reference one-shot observations are 123.389 / 119.961 ms, with reference-registration variation contributing. Independent CLI is 72.768 ms median / 74.061 p95 with identical diagnostics/WAV; this is entry-point validation, not the paired result. Temporary servers stop and both original supervisor PIDs remain unchanged/healthy.
- Post-timing profile retains 13,289 events: projection-only resident intervals 38.207 ms, projection/attention overlap 10.607 ms and attention-only 6.743 ms, all including waits. The 61.19-ms initial LLM interval remains dominant. `REPORT_ASYNC_WEIGHTS.md` records all alternatives, measurement caveats and reproduction. Next bounded hardware experiment: exact QKV projection/attention-preparation scheduling, guided by its roughly 6.37-µs post-wait interval and short actual dependency wait. The 50-ms goal stays active and unmet; no codebook reduction is justified.


## Clustered QKV preparation continuation (2026-09-20)

- The preceding register-preload pass was progress. Revalidated its 325-file source archive and idle H200 before continuing. Kept all 32 acoustic codebooks, streaming voice cloning, selected G32 arithmetic and both original services. Reviewed ClusterFusion and current multi-CTA/LLVM documentation; no paper speedup is extrapolated to this workload.
- Added one cluster per Q/K/V head, fusing norm/QKV projection, head norm, rotary and KV writes. The initial 52-configuration Triton 3.8 screen passes 5,616 checks, but selected-runtime transfer exposes changed reduction arithmetic. Explicit projection FMA ordering fixes a one-byte layer-2 discrepancy; a full request then exposes normalization reduction ordering at layer 30/fifth decode. Saved the failing input and reproduced the original local 32-value FMA chain. Zero-input sanitization later exposes rotary signed-zero differences; corrected to the selected PTX subtraction. Compiler/layout/export failures, unequal binaries and failed full-stream logs are preserved.
- Exported only the fused kernel with the isolated compiler and loaded its cubins through a checked CUDA-driver ABI; the rest of the model remains on Triton 3.7.1. Corrected final all-layer ring passes 216 comparisons and private graph checks, measuring 19.063 → 18.849 µs with 0.205 µs median paired gain. Final ring/request order is balanced; earlier two-mode ring rotation/reversal accidentally kept control first and is not the final evidence. Leader-only head preparation and larger/smaller hints do not improve full requests.
- Final corrected-binary twenty-pair TTFA is **74.227 → 74.144 ms**, p95 **74.850 → 74.719**, with **0.106 ms median paired gain** and 16/20 improvements. Two preceding complete comparisons gain 0.237 / 0.128 ms for the nominal schedule. Across the three successful comparisons all 124 measured complete streams and sampling states agree; all 52 controls match prior hashes. The final binary has 48 exact graph comparisons covering logits, IDs, RNG and all 72 poisoned KV buffers. Initial LLM work is still 61.080 ms; prefill and first codec remain about 6.83 / 4.53 ms.
- Added optional `--qkv-cluster` to serving, quality and benchmark entry points, before graph capture. The fresh-install test caught a missing optional capacity attribute, now handled using physical cache length. Both complete cloning suites pass: all 48 WAVs, eight reference copies and metadata match prior selected audio. Existing identical-audio diagnostic scores apply; no ASR rerun or human-quality guarantee is claimed.
- Memcheck, racecheck and synccheck each pass 72 production/debug private-graph cases including real/zero/spike inputs, positions 0/127/1023 and the saved trajectory regression. Zero errors, hazards or warnings. Four corrected cubins each use 64 registers per thread, 2 KiB shared memory per CTA and no spills; archived PTX/SASS show cluster barriers/reads and original dependency controls.
- Sequential temporary HTTP medians are **77.816 → 77.716 ms**, but p95 regresses **78.141 → 78.600**. All twenty PCM streams match; malformed-input and cancellation/recovery checks pass. Fresh-registration observations are 116.811 / 112.970 ms, largely affected by unchanged reference registration varying 35.604 / 32.458 ms. Independent CLI is 74.791 ms median with identical diagnostics/WAV, slower than the prior separate CLI observation and not a gain claim. Temporary servers stop; original PIDs 54030 / 56179 stay ready.
- Final post-timing profile removes 1,188 launches (13,289 → 12,101 events). Gate/up and down retain the largest projection resident intervals, with median 19.008 / 25.504 µs; these include waits and overlap, not utilization or a lower bound. Further cluster scheduling/resource placement and the gate/up/down chain remain concrete targets. `REPORT_QKV_CLUSTER.md` records implementation, corrections, measured limits and reproduction. The 50-ms goal remains active and unmet; no codebook reduction is justified.


## Down-projection tile and cluster-placement continuation (2026-09-20)

- Classified the preceding clustered-QKV pass as progress, not a blocker. Revalidated its 355-file source archive and the idle GPU before this pass. All GPU jobs run sequentially; original services remain unchanged.
- Sixteen cluster launch-policy/carveout variants pass 1,728 real-ring comparisons and sixteen private graph checks. A 50% carveout loses in complete requests. Load balancing's initial 0.110-ms paired gain fails to repeat (−0.018 ms); neither launch change is selected.
- Thirty down-projection tile/preload configurations pass 3,240 full-ring output/cache comparisons and thirty graph checks. The eight-row/four-warp scale-only tile improves the gate/up → down → selected clustered-QKV chain by 0.419 µs paired. Every full packed-weight preload loses. A failed comparison-helper pilot is retained separately; the corrected ninety-case pilot passes.
- Added optional `--down-tile8` in serving, quality generation and the ordinary benchmark. It installs a per-model callable before capture and requires the selected 32-codebook clustered G32 preset. It retains exact arithmetic, the FP32 codec, and original synchronization. Compiler audit: 128 registers, 2 KiB shared memory, zero spills.
- The four-mode comparison measures 0.589-ms median paired gain for the tile, 19/20 faster. The final two-mode repeat measures **74.233 → 73.773 ms** median, **0.447 ms paired gain**, 17/20 faster. Its p95 slightly regresses, **74.783 → 74.838 ms**. Across all three complete comparisons, 156 measured streams preserve PCM/RNG and 288 full-model graph cases pass; 52 control hashes match prior selected output.
- Memcheck/racecheck/synccheck each pass 72 changed-input, cache-poisoned and odd-row cases with zero errors or hazards/warnings. All 48 original/expanded bilingual cloning WAVs, eight reference copies and generation metadata match the prior selected suites. No quality tradeoff or new ASR/human claim.
- HTTP streaming/error/cancellation checks pass and all twenty PCM pairs match, but latency **regresses 75.754 → 77.418 ms median**, with p95 **76.958 → 78.497 ms**. Unchanged prefill/codec also run slower in the candidate process, consistent with runtime variation but not proof of its cause. No HTTP gain is claimed; the change stays opt-in. One fresh-reference observation is **114.908 / 115.102 ms**. Temporary servers stop.
- Ordinary CLI validation passes five requests at 74.299-ms median with exactly matching diagnostics and WAV. All 282 top-level Python modules parse. Original PIDs 54030/56179 remain ready; GPU returns to 0% utilization and 75,459 MiB baseline allocation.
- Final initial LLM interval is **60.564 ms**, still the dominant stage. Gate/up and down resident intervals remain largest, but include PDL waits/overlap. Next: retest producer-release points and integer/reduction layouts with the newly qualified down tile; do not infer an impossibility bound or reduce codebooks. `REPORT_DOWN_TILE.md` records the full evidence. The 50-ms goal remains active.


## MLP release/layout review and paired HTTP continuation (2026-09-20)

- Classified the preceding down-tile turn as progress. Revalidated all 378 archived source files and the archive hash; GPU was idle and original services retained PIDs 54030/56179. All GPU jobs stayed sequential.
- Screened 58 release/layout configurations, including control and three aliases of selected settings. All 174 pilot and 6,264 full-ring bytewise output/cache comparisons pass, as do 58 private graphs. Every changed schedule loses on the complete weight ring. Fewer registers alone do not help; the nearest gate/up layout drops 162 to 160 registers but doubles shared memory and loses latency.
- Full requests confirm rejection of implicit down release and gate/up IG1/IR2: paired changes −0.123/−0.187 ms, only 1/12 and 2/12 faster. All 36 measured streams preserve PCM/RNG, 96 full-model graph checks pass, and all twelve controls match prior hashes. No new selected kernel or serving flag.
- Built a temporary paired HTTP harness sharing weights/codec/reference encoder and restoring each variant's context/head/initial-audio graphs on the single GPU worker. Initial startup failed an existing context-manager guard; the corrected capture/restore setup passes. The failed source/log are retained and excluded.
- Cached-voice HTTP: **77.789 → 77.312 ms median**, **0.329 ms paired gain**, 15/20 faster. P95 regresses **80.281 → 81.018 ms**; the 103.690-ms candidate outlier stays. Internal engine gain is 0.538 ms paired. All forty cached streams match PCM/RNG, with twenty controls matching prior HTTP hashes.
- Fresh-reference HTTP registration plus synthesis: **110.933 / 109.967 ms** median across ten pairs, 1.204-ms paired difference and 7/10 faster. Internal engine paired gain is 0.471 ms; unchanged registration's paired difference is 0.376 ms. Do not attribute the full workflow difference to the tile or claim a new encoder gain. All twenty fresh streams match PCM/RNG.
- Variant/voice errors and cross-variant cancellation recovery pass (429 → 200). Temporary server stops. Selected source/kernels and original supervisor services remain unchanged. `REPORT_MLP_SCHEDULE.md` records the new evidence while preserving the prior separate-server regression.
- Read current Gluon/PDL documentation, Linear Layouts, and September 2026 Hopper-utilization/Leech-lattice serving papers. Their results are not MOSS speedup claims or a latency lower bound.
- Next hardware experiment: shard one gate/up 32-value activation-quantization group across two/four SM90 CTAs, exchange the small output via cluster shared memory, and preserve the existing exact quantizer. This targets the 162-register gate/up tile without duplicating its full projection. Reuse isolated Triton 3.8 cluster export and explicit legacy FP32 helpers; validate fixtures, real trajectories and synchronization before any integration. Keep all 32 acoustic codebooks. The 50-ms goal remains active.


## Gate/up clusters and exact compiler continuation (2026-09-20)

- Classified the preceding MLP/paired-HTTP turn as progress. Revalidated all 384 archived source files and archive hash; original services and all 32 acoustic codebooks remain intact. GPU jobs ran sequentially.
- Implemented SM90 gate/up clusters across one/two/four/eight CTAs, gathering 32 output values or reducing a distributed quantization maximum. Fixed two compiler layout-type failures; their source/logs remain separate. Explicit legacy FP32 order preserves the selected arithmetic under isolated Triton 3.8.
- Initial full ring passes 540 comparisons/five private graphs; expanded 29-mode ring passes 3,132 comparisons/29 graphs. Every multi-CTA variant loses: two-CTA 80-register chain is 38.252 versus 36.660 µs control. The selected one-CTA IG1/IR2 variant uses 148 registers, 2 KiB shared and zero spills, measuring 36.552 µs and 0.075 µs paired gain. Its compiler and layout change together.
- Full-request initial/repeat/final gains are 0.113/0.265/0.238 ms for IG1/IR2. Final twenty-pair median is **73.884 → 73.502 ms**, p95 75.321 → 74.775, 14/20 faster. All 136 measured streams preserve PCM/RNG, 52 controls match prior hashes, and 240 full-model graph cases preserve logits/IDs/RNG and all 72 poisoned caches. Initial LLM work remains 60.311 ms; prefill/codec about 6.825/4.531 ms.
- Added optional `--gateup-compiler` in serving, quality and ordinary CLI paths, before graph capture and requiring the selected eight-row/clustered G32 preset. A four-cubin bundle copies the selected binary hashes unchanged from the full screen; other kernels remain on Triton 3.7.1. No production default changes.
- Memcheck/racecheck/synccheck each pass 252 changed-input/private-graph cases, zero errors or hazards/warnings. All 48 original/expanded bilingual cloning WAVs, eight references and metadata remain exact. No new ASR or human-quality claim.
- Paired HTTP cached median **77.401 → 77.102 ms**, 0.210-ms paired gain, 13/20 faster; p95 78.892 → 78.500. Fresh median 111.810/110.754 ms, paired 0.329 ms, 6/10 faster; p95 regresses 115.606 → 123.279. Fresh internal engine gain is 0.160 ms and unchanged registration varies by 0.213 ms paired. All sixty streams preserve PCM/RNG; error/cancellation recovery passes and temporary server stops.
- Ordinary CLI passes five requests at 74.410-ms median with identical 36-step diagnostics and WAV; this separate-process observation is slightly slower than its predecessor and not a speedup claim. Production-flag HTTP smoke validation is recorded separately.
- Selected profile retains 12,101 kernel events, with gate/up/down resident medians 18.849/25.120 µs. These include dependency waits and overlap. Next: targeted register allocation around the new one-CTA kernels, then consider a cooperative G32 chain with verified residency/synchronization. Prior caps used different kernels/compiler; no latency lower bound or codebook reduction is justified. `REPORT_GATEUP_COMPILER.md` contains evidence and reproduction. The 50-ms goal remains active.

## CUDA resource continuation (2026-09-20)

- Revalidated all 945 preceding source/binary files and archive SHA before work. Retained all 32 codebooks, streaming cloning, G32 decode, BF16 prefill and FP32 codec; GPU jobs ran sequentially.
- CUDA 13.0 PTXAS reassembled two exact Triton 3.8 one-CTA layouts with register caps, static shared allocation and shared spilling: 26 configurations, 104 production/debug/residual binaries. Driver residency limits, local/shared allocation, compiler spill bytes and null Triton spill counts remain distinct in the manifest.
- The 81-case pilot and 2,916-case all-layer ring pass. Cap-144 IG2/IR4 improves the ring by 0.130 us paired; most caps/shared-spill schedules lose. Cap-128 IG1/IR2 shared spilling removes local allocation but remains 1.295 us slower than selected.
- Full cap-144 paired gains are only 0.087/0.105/0.068 ms over 12/20/20 groups. Final medians are 71.238/71.178 ms, p95 73.779/73.893. Unchanged stages also enter a faster runtime phase; the absolute difference from prior approximately 73-ms runs is not a kernel gain. Candidate remains benchmark-only; selected gateup_compiler code/binaries and services stay unchanged.
- All 136 measured complete PCM streams and RNG states match; 52 control hashes match prior output; 240 full-model graph cases preserve logits, IDs, RNG and all 72 poisoned KV buffers. Memcheck/racecheck/synccheck each pass 252 cases with no reported errors/hazards/warnings.
- Final profile still targets LLM projections: gate/up/down resident medians 18.912/25.760 us, including waits and overlap. These are not hardware utilization or a lower bound. Read recent ForgeMegakernel, Ada-MK and EventTensor primary papers; finer producer/consumer dependencies and persistent G32 scheduling remain hypotheses, not implemented results.
- REPORT_GATEUP_RESOURCES.md and gateup_resources_pass_summary_v1.json retain all trials and limitations. No new serving flag, HTTP/fresh-reference timing or 48-utterance quality claim. Existing qualified measurements remain authoritative for serving. No proof supports reducing codebooks, and the 50-ms goal remains active.

## Historical-KV overlap continuation (2026-09-20)

- Classified the resource sweep as progress, revalidated all 1,565 source/binary files and archive SHA, and kept all 32 codebooks. GPU experiments remained sequential. No driver or existing service changed.
- Implemented CUDA historical K/V preloads before the attention dependency wait. Only rows strictly before the current token may be loaded early; Q/current K/V remain after the wait. Position/history producers precede QKV, which waits upstream before releasing attention. No new inter-kernel spin protocol or assumption of concurrent execution.
- The first read-only-load build moved cache loads after the wait. Explicit volatile global-vector PTX fixes the scheduling, confirmed in SASS. A guard rejected an older QKV bundle; fresh early-release cubins retain current signed-zero behavior. Initial memcheck encountered a host None-residual validator bug; corrected runs supply the evidence.
- Thirty schedules pass 3,240 all-layer intermediate/cache comparisons and 240 private-graph boundary checks. Selected FP32 K/V staging with early QKV/attention release reduces the chain from 18.816 to 17.104 us (1.709 us paired). Native kernel: 98 registers, 2,080 shared bytes, zero spills. Early producer: 72 registers, 2 KiB shared, zero spills.
- Full streams improve 73.423→71.706 ms in twelve triplets, then 73.636→71.856 ms in twenty pairs, with 1.750/1.685 ms paired gains. Repeat is faster in 19/20, retaining an 80.543-ms candidate outlier. Initial 32 LLM steps fall 60.409→58.587 ms; prefill/codec are unchanged. All 76 streams preserve PCM/RNG; 32 prior control hashes and 144 whole-model graph cases match.
- Integrated opt-in --attention-history in server, quality and benchmark entry points before graph capture. It requires the qualified gateup_compiler/down8/QKV preset. All 48 bilingual cloning WAVs, eight references and generation metadata match. Ordinary CLI: 72.211 ms median/72.352 p95, same 36-step diagnostics and WAV. Actual production-flag HTTP smoke with a fresh voice passes and stops its server.
- Memcheck/racecheck/synccheck each pass 360 changed-history/position cases across four schedules, layers 0/17/35 and capacities 128/1024. A supplementary selected-production-producer sanitizer check is recorded separately; the broad runs expose QKV intermediates for comparison.
- Paired HTTP cached TTFA: 76.436→75.022 ms, 1.919 ms paired gain, all 20 faster. Fresh registration+synthesis: 112.128→110.665 ms, 2.757 ms paired gain, 9/10 faster. Internal engine paired gain is 1.931 ms in both; unchanged reference encoder contributes 0.461 ms paired variation. All 60 PCM/RNG streams, prior cached controls, invalid-input checks and 429→200 cancellation recovery pass. Temporary server stopped.
- Post-timing profile shows 1,188 overlapping QKV/attention resident pairs (median 7.904 us); overlaps include waits, not measured useful work. Gate/up/down remain dominant projection intervals. REPORT_ATTENTION_HISTORY.md and attention_history_pass_summary_v1.json preserve evidence and reproduction. The goal remains active and no evidence justifies reducing codebooks.
- Supplementary selected-production QKV runs completed: memcheck/racecheck/synccheck each pass 90 additional cases with no errors/hazards/warnings, bringing aggregate sanitizer coverage to 450 cases per tool. The broad debug-output and separate production checks remain individually recorded.
